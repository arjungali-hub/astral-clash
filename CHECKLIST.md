# Astral Clash — Build Brief Checklist

Tracks every item from the build-brief prompt. Updated after every batch of changes to `index.html`.

**Time-scaling approach (Batch 1 decision):** using the `dt` multiplier model (dt = 1.0 at 60fps), not a full frames→seconds conversion. Existing per-frame tuning constants stay valid multiplied by `dt`, rather than rewriting every constant into real-world units.

Legend: `[x]` done · `[~]` partially done (see note) · `[ ]` not started

## Regression found and fixed outside the batch list

**The game was completely unplayable after the previous update** — the fighter-select grids were empty and the Bot toggle button did nothing. Root cause: `onWindowResize()` was called immediately at setup time (to size the canvas on first load) but reads `camera`, which is a `const` declared *later* in the script — a temporal-dead-zone `ReferenceError` that threw before `buildGrid()` or the bot-toggle listener ever ran, silently halting the rest of the script. `node --check` never caught this because it only validates syntax, not runtime references — confirmed by actually executing the script under a mocked DOM/THREE harness. Fixed by moving the initial `onWindowResize()` call to after `camera` is constructed. This wasn't on the brief's list because it was introduced by the huge feature-update batch, not by anything the brief was written against — noting it here since it directly affects the "no console errors" verify item for every batch since.

**Special meter always showed NaN and never charged.** Root cause: `Fighter.meterRateMult` was read in two places (passive regen and on-hit meter gain, both part of Batch 4's unified meter-generation rework) but the constructor never actually assigned it from the character config — so it was `undefined`, `undefined * anything` is `NaN`, and once `specialMeter` went `NaN` it stayed `NaN` forever (any further arithmetic on a `NaN` is still `NaN`), which is exactly the "shows NaN and won't charge" symptom reported. This is a case the mock-DOM harness can't catch — `undefined * x` doesn't throw, it just silently produces `NaN`, so the script "ran fine" by the harness's own definition even with this bug present; it needed an actual human noticing wrong-looking output. Fixed by adding the missing `this.meterRateMult = cfg.meterRateMult;` in the constructor, alongside the other per-character stat assignments.

**Training mode removed at the user's explicit request** after being built in Batch 9 — see that batch's section below for what was deleted.

**Per-attack weapon animation added**, on request, beyond the whole-mesh lean Batch 8 shipped. Each character's mesh builder now stashes a `weapon` part (or parts — Voss's twin daggers move together) and a `weaponAnim` config; `initMesh()` captures each part's rest transform once, and a new `animateWeapon()` (called every frame from `render3D`, layered on top of the existing lean) drives a real per-character swing: Kaelen and Voss get a horizontal slash/stab arc (rotate around Y), Draven gets a heavy overhead swing (rotate around Z), Gorgonok gets a straight punch thrust (position offset along local X), and the two ranged casters (Lyra, Seraphine) get a charge-and-release scale pulse on their shard/halo instead of a melee swing, since they don't have a weapon part to swing. All three animation types share one generic startup→active→recovery progress curve (wind-up extreme → follow-through extreme → ease back to rest) rather than six bespoke per-character animation functions.

**The "skill" action (third attack, on top of basic/special) removed at the user's explicit request.** This was a bigger removal than it looks — every character had a unique skill (Kaelen's Twin Strike, Lyra's Glass Step, Gorgonok's Ground Pound, Voss's Smoke Step, Draven's Bulwark Stance, Seraphine's Radiant Pulse), so this touches most systems in the file: `CHARACTERS` config (`skill2Id`/`skill2Name`/`skill2Desc`/`skill2Cooldown` fields), `FRAME_DATA` (the `skill` startup/active/recovery sub-object per character), the whole `Fighter.doSkill()` method and its six-case switch, `tryStartAction`/`updateActionState`'s skill branches, `skill2Cooldown`/`skill2CooldownMax` fields and their per-frame tick, the HUD's skill-cooldown bar and "Skill Ready/Xs" label, the feet-level `skillReadyRing` ability pip, the fighter-detail panel's Skill ability row, key bindings (P1 `H`, P2 `;`, gamepad Y button) and their rebind-screen/controls-summary entries, bot AI's `skillChance`/skill-trigger logic in `BOT_DIFFICULTY`, and the skill wind-up sound-pitch case. One side effect worth flagging: Draven's Bulwark Stance was his *only* source of the shield/damage-reduction mechanic (`shieldFrames`), so removing it means Draven no longer has any damage-mitigation ability — his `shieldFrames`/`shieldRing`/`shieldReduction` infrastructure is still fully in place and still used by Seraphine's Astral Ward *special* (which independently grants a shield and was never a skill), just no longer reachable by Draven. Left his flavor text ("nearly unbreakable tank") as-is rather than rewriting character descriptions, which felt like scope beyond "remove skills."

**Passive HP regen ("healing over time") removed at the user's explicit request.** `cfg.regen` (a per-character passive-regen rate) and the `this.hp += this.regen * dt` tick that ran it every frame have both been deleted, along with the `regen:` field from all six `CHARACTERS` entries. This only ever restored a fraction of a percent of HP per frame passively — it did *not* touch the pickup heal (`PICKUP_HEAL_PCT`), Seraphine's Astral Ward special's one-shot heal-on-cast, or the now-removed Radiant Pulse skill (already gone from the skill removal above) — those are instant, on-cast/on-pickup heals, not "over time," and weren't asked to go.

**Projectiles passed straight through staircases.** Root cause: the projectile-collision check in `tickEffects()` only tested against `OBSTACLES` (pillars) and `PLATFORMS` (one-way floors, blocked only when the shot is below the platform's underside) — `TERRAIN` (steps/ramps) was never checked at all, so a low-flying shard/orb clipped straight through a stair's solid riser. Fixed by adding a third check: a projectile below a `step`-type terrain entry's height and within its `hw`/`hd` footprint is now blocked, same style as the existing platform check. Ramps are deliberately excluded — they're a smooth walkable incline with no vertical face to collide against, so "blocking" one would need slope-interpolated height math for a case that isn't really "a shot going through a wall" the way a stair riser is.

**Start screen (fighter-select menu) UI improved, on request.** The middle VS column had become a dumping ground for every menu-level setting added since Batch 9 (Rebind Keys, HUD Text scale, Audio Settings, stacked four-deep in a 110px column) — it competed with the actual P1-vs-P2 matchup for attention and made the "pick, pick, fight" flow harder to read at a glance. Changes: moved Rebind/HUD-scale/Audio into a new `.menu-footer` row (a muted, secondary-styled button strip below the whole select screen, visually separated by a top border) so the VS column only holds what's actually about the upcoming fight (VS label, status, bot difficulty). Each side panel's Bot-toggle/Random buttons moved from two stacked full-width buttons into one `.side-actions` row, saving vertical space. `#vs-status` gets a `.ready` state (bigger, cyan, gently pulsing via a CSS animation) toggled from `refreshMenuUI()` the instant both sides are confirmed, instead of the "Fight!" text just quietly changing color like every other status message. The `<h1>` title got a size/letter-spacing bump and a small gradient underline (`.brand-divider`) for a bit more visual presence instead of a single flat text-shadow line. Not visually verified in an actual browser — this is CSS/DOM structure only, checked for JS correctness (the `.ready` class toggle) via the usual syntax+mock-harness pass, not for how it actually looks.

**You could walk straight up any terrain, no matter how tall, without jumping — including onto Sundered Stair's tallest (54-unit) step from ground level.** Root cause: `groundLevelAt()` snaps a grounded fighter's `z` straight to whatever height is under their new feet every frame, with no limit on how big that jump in height could be — so sidestepping into the base of a tall step teleported you onto its top exactly the same way a gentle few-unit lip would. Fixed with a new `Fighter.resolveTallTerrain()`: while grounded, a move that would raise you more than `MAX_WALK_STEP_UP` (20 units) in one step now gets blocked like a wall instead of auto-climbed — tried as a full move, then X-only, then Y-only, then reverted entirely if none of those clear it (same axis-separated slide-along-the-wall approach `resolvePlatformWalls` already used for platforms). Sundered Stair's individual risers are 18 units each, under the cap, so climbing the actual staircase step-by-step is untouched — only skipping straight to a tall step from the side, or off the correct path, now requires an actual jump. Bot AI's "purposeful jump" check (Batch 6) is extended the same way, so bots don't stall at the base of terrain they now can't just walk up.

**Sundered Stair's staircase wasn't aligned with the (west) arena wall.** It sat with ~85 units of unexplained flat floor between the arena boundary and the base of the first step, reading as floating/misplaced rather than built into the level. Repositioned so the first step's near edge sits flush on `ARENA_LEFT`. Folded into the broader map-rescale pass below.

**Maps made bigger, on request, plus more maps and more/taller terrain.** The virtual coordinate space (`VIRTUAL_W`/`VIRTUAL_H`) grew from 900x500 to 1170x650 (a uniform 1.3x) — every hardcoded pixel position that assumed the old space (canvas attributes, `#game-container`'s CSS `aspect-ratio`, the P2 HUD panel's x-offset, camera position/framing distances, the directional light's shadow-camera frustum, fog near/far) was updated to match; arena-bounds-derived values (`ARENA_LEFT/RIGHT/TOP/BOTTOM`, `ARENA_CX/CY`, the floor plane, rim-edge trim, `PICKUP_SPOTS`, hazard placement) needed no changes since they were already computed from `VIRTUAL_W`/`VIRTUAL_H` rather than hardcoded. All four existing maps got a uniform 1.3x rescale of every horizontal coordinate (position/footprint) — **heights were deliberately left unscaled**, since vertical reach is governed by `JUMP_VZ`/`GRAVITY` (unchanged), not by how wide the map is; scaling both would have thrown off which platforms are jumpable. Two brand-new maps were added (**Skyreach Spire**: a three-tier platform staircase climbing to a height-200 "throne" that's flatly unreachable in a single jump from the ground — has to be climbed tier by tier, with the last gap deliberately tight for the slowest character; **The Shattered Bridge**: two ground shores connected only by a broken chain of floating stepping-stones, the center one sitting right at the edge of single-jump reach). Every existing map also picked up at least one new terrain/platform piece it didn't have before (shallow rises on Astral Proving Grounds, which previously had zero terrain; a central step on Twin Ramparts; a hard-to-reach side platform on The Colosseum Ring; Sundered Stair's fixed staircase gained a fourth tier plus a two-platform chain leading to a genuinely hard-to-reach height-160 platform, unreachable except by climbing the stair first). All six maps' spawn points and geometry (arena-bounds containment, obstacle/platform/pillar overlaps) were verified with a standalone script exercising the game's own `assertSpawnsClear()` plus additional overlap checks — not visually confirmed in a browser, since none is available here.

**Jumping had no cooldown at all — you could jump again the instant you landed.** Added `Fighter.jumpCooldown` (new `JUMP_COOLDOWN = 60` constant — noticeably longer than a jump's own ~43-frame airtime, so landing doesn't mean you can immediately jump again), ticked down alongside the existing `attackCooldown`/`dodgeCooldown` pattern and gating the jump trigger the same way. Surfaced in the `?debug=1` per-fighter state readout (`jumpCD:`) next to the existing `dodgeCD:`.

**Characters rebuilt as actual humanoid figures, on request, with attack/special animations reworked to match.** Every character used to be an abstract shape (a box/octahedron/sphere "body") with a weapon prop bolted on and animated in isolation (the prop rotating/thrusting/pulsing on its own, unconnected to any body part). New shared `buildHumanoidBase(color, opts)` builds an actual person-shaped rig — two legs, a torso, a head, and two shoulder-pivoted arms each ending in a hand socket — reused by all six character builders, which now attach their weapon(s) to a hand instead of floating them in space. `animateWeapon()` was rewritten from three prop-level animation types (rotate/thrust/pulse) down to two rig-level ones: **'swing'** rotates one arm's shoulder pivot (or an array of pivots — Voss's twin daggers stab together, Draven's two-handed hammer grip swings both shoulders in sync) through a wind-up-then-strike arc, so an attack now reads as an arm actually swinging a held weapon; **'cast'** raises the casting arm and holds it through the whole active window (rather than swinging back down mid-cast like a melee follow-through would) while flaring the held prop's scale, for the two ranged casters. Lyra keeps her ambient orbiting shard (attached to the main body, not a hand, so it doesn't fight the new arm-swing transform) alongside a second, smaller shard actually held in her casting hand; Seraphine keeps her halo the same way. Draven's old shoulder-armor pad detail was dropped rather than reworked to fit the new rig, given the scope already involved. Verified with `node --check` and the mock-DOM runtime harness (both clean); not visually confirmed in a browser, since none is available here — proportions, weapon-hand placement, and swing angles are a reasoned first pass, not a rendered/previewed result.

**Tutorial ("How to Play") added, on request.** New `#tutorial-screen` overlay, styled to match the game's existing card/panel visual language (dark slate panels, cyan accents) rather than a plain wall of text — a responsive card grid (`.tutorial-grid`, auto-fits down to one column on narrow widths) covering Objective, Movement & Jumping (including the new jump cooldown), Basic vs. Special (including how meter actually builds), Dodge, Hazards & Pickups, and a Controls card. The controls card is generated from the same `controlsSummary()` helper the pause-screen reminder already uses, so a rebind shows up correctly there too instead of a hardcoded list going stale. A prominent bordered "How to Play" button sits right under the title (separate from the muted settings-button footer, since this is the one thing a brand-new player most wants), and it auto-opens the first time the game loads in a browser — and every load after that — until the "don't show automatically" checkbox is checked, persisted via the same `safeLSGet`/`safeLSSet` helpers as the other settings. Verified with `node --check`, the mock-DOM harness, and a manual `<div>` open/close tag-balance check across the markup; not visually confirmed in a browser. **STANDING RULE going forward (user asked to remember):** every mechanic change must also update this tutorial's cards so it never drifts out of sync.

**Damage multiplied 5x, on explicit request.** Every character's basic-attack `dmg` and every special's hardcoded damage number were multiplied by 5 (Kaelen 7→35, Lyra 6→30, Gorgonok 10→50, Voss 5→25, Draven 14→70, Seraphine 5→25; specials scaled to match, e.g. Draven's Ground Breaker 45→225). Deliberately makes time-to-kill much shorter — works with the arena-collapse mechanic to force decisive fights. `STAT_MAX.dmg` (the select-screen stat-bar normalizer) was bumped so the bars still read sensibly.

**Round timer removed and replaced with Arena Collapse, on request.** There's no clock counting down to a timeout draw anymore — the whole timeout branch (`decideRoundResult(byTimeout)`, the `roundTimeLeft <= 0` path, `ROUND_TIME`/`roundTimeLeft`) is gone. Instead: new constants `SHRINK_INTERVAL`/`SHRINK_STEP`/`CRUSH_GAP`/`CRUSH_DPS` and state `shrinkTimer`/`shrinkAmount`/`playLeft`/`playRight`/`crushing`. Every `SHRINK_INTERVAL` seconds `updateArenaShrink()` pulls the playable left/right bounds inward by `SHRINK_STEP`; `clampToArena()` now reads `playLeft`/`playRight` (the shrinking bounds) instead of the fixed `ARENA_LEFT`/`RIGHT`, so fighters are physically squeezed toward center. Two glowing red wall-slab meshes (`shrinkWallLeftMesh`/`shrinkWallRightMesh`, `syncShrinkWalls()`) visualize the closing bounds. Once the gap hits `CRUSH_GAP`, the walls snap shut (`crushing = true`), input/combat freeze, and both fighters lose a **flat, equal, max/current-HP-independent** `CRUSH_DPS` per second (`hp -= CRUSH_DPS * dt/60`) until one dies — reusing the existing `hp<=0 → DEATH` flow so "whoever dies first loses" falls out of logic that already existed. `render3D` squeezes each body flat along X while crushing. The HUD's timer readout became "Walls close in Ns" / "CRUSHING!". State reset in `startRound()`. `decideRoundResult()` simplified to no longer take a `byTimeout` arg.

**Aim-lock removed, on request.** The modifier that let you move one way while facing another (`aimlock` binding, the `aimLock` local in `update()`, the `if (!aimLock) setFacing(...)` guard) is fully gone from bindings, both input branches, the controls summary, and the rebind action labels. Movement always sets facing now.

**Character models, weapons, projectiles, and attack animations overhauled + 4 new characters added, on request.** Addressed several things at once. (1) *Weapons no longer look like sticks/dots:* new prop builders (`buildSwordProp`/`buildDaggerProp`/`buildHammerProp`/`buildScytheProp`/`buildFistProp`/`buildStaffProp`/`buildWhipProp`/`buildOrbProp`) build weapons from shaped primitives — flat tapered blades with a crossguard, pommel and pointed cone tip; a bulky boxy hammer head banded on a handle; a curved multi-segment scythe hook; glowing knuckled gauntlets; a staff with a crystal head and prongs; a drooping segmented vine-whip. (2) *Projectiles look like projectiles:* `syncProjectiles()` rewritten from a plain sphere "dot" to an oriented energy bolt — an elongated crystalline core stretched along travel, a trailing cone tail, and a shimmering glow-ring halo, rotated each frame to point the way it's flying (`p.big` scales up special/overcharged bolts). (3) *The three look-alike animation pairs now differ:* `animateWeapon()` was expanded from 2 motion types to 4 (`swing` with an optional body-`twistDeg`, `stab`, `flurry`, `cast`) and re-assigned so no two similar weapons animate alike — Kaelen does a wide body-twisting horizontal cleave while Voss does a rapid in-line double `stab`; Gorgonok throws a single low-windup rising uppercut while Draven does a slow huge two-handed overhead smash; Lyra thrusts one arm forward to hurl a shard while Seraphine raises *both* arms overhead in an invocation. (4) *Four new characters:* **Nyx** (Umbral Reaper, long scythe, wide reap that yanks the enemy in; sweeps opposite Kaelen so the two blade users read differently), **Ignis** (Emberfist, dual gauntlets, rapid alternating-arm `flurry` jabs distinct from Gorgonok's single punch; fiery AOE special), **Aurelia** (Storm Herald, ranged lightning staff firing oriented bolts; a massive overcharged `big` bolt special), **Thorne** (Thorncaller, longest-reach vine-whip, overhead crack; wide bramble-lash special). Each got a full `CHARACTERS` entry, `FRAME_DATA` (basic+special S/A/R), mesh builder + `MESH_BUILDERS` registration, `doSpecial` case, `SFX_TYPE_BY_CHAR` mapping with new synthesized impact sounds (scythe/bolt/whip), and new melee-effect handling (`scythe`/`whip` added to the arc-effect branch and `effectColor` map). Select-screen support extended from 6 to 10: `P1_KEYS`/`P2_KEYS` grew to 10 entries each, `STAT_MAX` rescaled so the stat bars normalize sensibly against the new (post-5x-damage) values, and the heading key-hints were de-hardcoded. Verified with `node --check`, the mock-DOM harness, **and a dedicated standalone script that builds all 10 character meshes and runs each through a full startup→active→recovery animation cycle for both basic and special** (all 10 pass). Not visually confirmed — geometry proportions, weapon placement, animation arcs, and projectile look are reasoned first passes, unrendered. Tutorial updated with a new "The Roster" card per the standing tutorial-sync rule.

**Start screen UI overhauled, on request ("take your time on the art").** Fixed the menu-footer buttons being left-aligned instead of centered (they had a fixed width but no auto-margins — now `margin: … auto …`). The bot-difficulty selector now only shows while at least one side is set to Bot (hidden via `refreshBotUI()` otherwise). **Bot sides no longer pick a fighter by hand at all** — a side toggled to Bot swaps its fighter grid for a "Bot Fighter — randomly chosen when the match starts" card (`.fighter-bot-mode`), and its actual fighter is rolled randomly in the new `beginMatch()` at match start (hidden until then), per request. New `sideReady()` (confirmed-or-bot) + an explicit pulsing "Start Match" button that appears once both sides are ready (the start trigger for bot-vs-bot or toggle-after-confirm cases; a human confirming as the final action still auto-starts). Art pass: radial cyan/violet vignette on the overlay background, gradient panel fills with a glowing top-accent line (`.menu-section::before`) echoing the in-game neon-map trim, a larger italic glowing "VS" badge with a radial halo behind it, and a brighter pulsing Start button. Verified via `node --check`, the mock-DOM harness, and `<div>`/`<button>` tag-balance checks; visuals unconfirmed (no browser).

**Verification caught a real balance bug: a landed special instantly refilled its own meter.** Found while writing a combat-verification script (see below) that actually calls `doBasicAttack`/`doSpecial` against a live opponent instead of only checking that the mesh/animation code doesn't throw. Root cause: `takeDamage()`'s "attacker builds meter from damage dealt" logic (the Batch 4 comeback mechanic) applied to *all* damage sources, including a special's own hit — and specials deal enough damage (175-225, after the 5x pass) that `amount * METER_DEALT_RATE * meterRateMult` clamps straight back to 100 the same frame the meter was spent, for any special that connects via an omnidirectional AOE check (`distBetween`) or a melee cone that happens to be facing the target. This existed in a smaller form before the 5x damage change (a ~76% partial refill) but the 5x pass turned it into a full, instant, always-on refill — specials could chain back-to-back forever once the first one landed. Fixed by gating the attacker's own meter gain on `attacker.atkKind !== 'special'` — a special's damage still lands and still builds meter for the *defender* (the taken-damage comeback mechanic is untouched), it just no longer pays for its own recast; only basic-attack damage (plus the passive trickle) rebuilds a spent special bar now.

**New verification tooling: a live combat-exercise script**, added alongside the existing mesh/animation-only check, because that check couldn't have caught the bug above (it never calls `takeDamage`). `combatcheck.js` constructs a real `Fighter` pair per character, calls `doBasicAttack()` (confirming a melee effect or projectile is produced) and `doSpecial()` with the meter and `atkKind` set to match how `updateActionState()` actually invokes it in-game (confirming the meter is consumed), for all 10 characters. All 10 pass. This directly exercises the brief's own "every character's attack/special fires, connects, shows an effect" verification item, which no prior tooling in this file actually tested end-to-end.

**Bot AI's ranged-vs-melee detection missed the new `bolt` attack type**, found by the same pass — Aurelia (added this session, `atkType: 'bolt'`) would have played like a melee bot (walking into range, never kiting) instead of a ranged one, since `botDecide()`'s `isRanged` check only listed `'shard'`/`'orb'`. Added `'bolt'` to the check.

**Select screen's top and bottom were cut off with no way to reach them**, reported after the roster grew to 10 fighters. Root cause: `#ui-overlay` is a `justify-content: center` flex column with no overflow handling — centering content that's taller than the viewport clips *both* ends equally, and there was nothing to scroll to reach the clipped parts (the inner `.select-row` had its own `overflow-y: auto`, but that only scrolled the fighter grid itself, not the brand header above it or the settings footer below it). Fixed by switching `#ui-overlay` to `justify-content: flex-start` with `overflow-y: auto` — top-aligned and scrollable, so a taller-than-viewport select screen is fully reachable by scrolling instead of silently clipped at both edges. Removed the now-redundant nested scroll on `.select-row` (a scroll region inside a scroll region just fights the mouse wheel).

**The three footer settings buttons (Rebind Keys / HUD Text / Audio Settings) consolidated into one, on request.** New `#settings-screen` overlay (same popup pattern as the existing Rebind/Audio screens) holding all three as a vertical list; the footer now has a single "⚙ Settings" button. Rebind Keys and Audio Settings still open their own pre-existing screens unchanged (same element IDs, same handlers) — clicking either from within Settings just also closes the Settings panel first so they don't stack.

**Fighter-select grid changed from 1 column to 2, on request.** `.fighter-grid`'s `grid-template-columns: 1fr` → `1fr 1fr`. With 10 characters this also roughly halves the grid's height (5 rows instead of 10), which helps the just-fixed select-screen scroll situation above — less content to scroll through in the first place. Pure CSS change; `.fighter-btn` needed no adjustment since it already fills its container width (the global `button { width: 100% }` rule), it just now fills a half-width grid track instead of a full-width one.

**Select-screen UI scaled up ~15%, on request ("fit perfectly with the bigger box").** Root cause of the mismatch: `#game-container`'s on-screen size grew when the maps were made bigger (`min(96vw, 1170px)`, up from 900px), but the HTML/CSS select-screen UI inside it is independent of that — none of its font sizes, panel widths, or paddings are tied to `VIRTUAL_W`/`VIRTUAL_H` (those only govern the WebGL/HUD canvas coordinate system), so the UI kept its original pixel sizing and now reads as too small for the bigger box around it. Bumped the main visual anchors roughly 12-18%: `h1`/`.vs-label` 2rem→2.4rem, base `button` 1rem→1.08rem with more padding, `.side-panel` width 290px→340px, `.fighter-btn`'s key badge/name/title text, `.bot-toggle`/`.menu-footer`/`.btn-tutorial` buttons, and the gaps between panels (14px→20px) and grid cells (4px→7px) all scaled with it. Pure CSS, no layout structure changes.

**Arena Collapse reworked to close on all four sides + map edges "fall away" instead of red walls, on request.** Three changes to the collapse mechanic: (1) It now shrinks **X and Y proportionally** (all four bounds pull toward center by the same fraction each tick, keeping the arena's shape) instead of only left/right. New `shrinkFrac`/`shrinkTargetFrac` (a 0→1 collapse fraction, smoothly eased toward a stepped target so the walls read as continuously closing) drive `arenaBoundsFromFrac()`, which sets `playLeft/Right/Top/Bottom`; `clampToArena()` now uses all four. (2) The old glowing red wall slabs are **gone** — instead the floor plane shrinks to the current bounds and any map geometry (pillars/steps/ramps/platforms) now outside them **drops straight down into the void and vanishes** ("as if it never existed"). Required making every map-builder (`buildPillarMesh`/`buildStepMesh`/`buildRampMesh`/`buildPlatformMesh`/`addRimEdge`/`addCornerGlow`) return its group and building them positioned via a group origin (not baking `worldX/worldZ` into child positions) so each can be dropped as a unit; `loadMap()` tracks them in `mapEdgeObjects` (tagged with game x/y) + `borderDecor` (rim/corner decor, hidden the instant collapse starts); new `syncArenaCollapse(dt)` scales the floor and culls out-of-bounds objects. (3) On the final shut it stays fully visible (no obscuring props) and each fighter is **crushed in place on their own side** — `render3D`'s crush squish now flattens the footprint in both X and Z while popping up in Y (a can-being-stepped-on look) rather than only flattening X. Verified with a standalone `collapsecheck.js` that simulates 120s: confirms both axes shrink to center, crush triggers (~54s of a fully-stalled fight), and all edge objects fall away.

**Bug fixed: standing on a tall platform "teleported" you off it.** Root cause: the platform-landing check in `update()` required `!this.grounded && this.z >= p.height`. The instant you landed (grounded = true), the next frame's gravity dipped your z a hair below `p.height`, so *neither* condition held, the platform stopped counting as ground, and you fell straight to the floor. Fixed by replacing that with a "descending onto the surface" test using a small `SURFACE_EPS` (5 units): you count as standing while within a few units of the surface (absorbing the per-frame gravity dip), while a fighter at ground level far below a tall platform still can't qualify — preserving the one-way "must jump up to it" behavior without the fall-off. Verified with `platformcheck.js` (fighter stays at z=130 on a height-130 platform for 180 frames, never dips).

**Bots massively improved at ranged shooters + new 'insane' difficulty, on request.** The core ranged-bot bug: `update()` always set facing from *movement* direction, so a kiting ranged bot (moving away from its target) faced backward and fired its projectiles away from the opponent — they were nearly useless. Fixed by giving bots an internal aim override (`botFaceX/botFaceY`, the bot-only equivalent of the removed player aim-lock) that `update()` applies *after* movement, so a bot always faces — and shoots at — its target regardless of which way it's moving. Ranged bots additionally **lead** the target (aim where a speed-7 projectile will intercept it, using the opponent's velocity), so shots connect against a moving target; verified with `rangedbotcheck.js` (13 shots toward a weaving opponent, 0 away — previously all away). Difficulty tiers gained `lead`/`strafe`/`relentless` flags: higher tiers strafe perpendicular to be harder to hit and passively slip shots, and don't waste airtime on idle jump-twitches. New **'insane'** tier (added to `DIFFICULTY_ORDER`, cycled by the difficulty button): near-instant reactions (2-6 frame think gap), ~98% attack / 90% special chance, always leads, aggressive strafing, dodges projectiles from further out, punishes recovery — a genuine boss.

**Specials redesigned: less damage, much more varied, and shorter-ranged where noted, on request.** The old specials were mostly "big damage in a circle/cone" (175-225 damage each — 2 specials killed most characters) and interchangeable. All ten were rebuilt around a *distinct mechanic* with far lower damage, so a special is a strong tool, not an instakill: Kaelen **dashes through** the enemy (~90, repositions behind); Lyra sprays a **wide 5-shard zoning fan** (~24 each); Gorgonok **launches** the enemy away + stuns (~70); Voss **blinks in and rapid-stabs** 4× with i-frames (~76 total); Draven's Ground Breaker is now **short-range** (85 units, halved from 170 as requested) with a **hard knockdown-stun** (~95); Seraphine's ward is **purely defensive** (shield + heal, no damage); Nyx **pulls the enemy in and roots** them (~70); Ignis leaves the enemy **burning over time** (45 + ~66 DoT); Aurelia's Thunderstrike is a **targeted bolt that can't be sidestepped** (~90); Thorne **ensnares/roots** the enemy in a wide arc (~60). Two new status mechanics were added to support these: `rootedFrames` (can't move, can still attack/turn) and `burnFrames`/`burnDps` (damage-over-time, ticked in `update()`), both dodge-negated via a `canAffect()` i-frame check. All ten specials verified to still fire (meter consumed) via `combatcheck.js`. Tutorial's "Arena Collapse" and "Basic vs. Special" cards updated to match, per the standing tutorial-sync rule.

**Healing and hazard zones removed; arena collapse retimed, on request.** (1) *No more healing:* the heal-pickup system (the green orb — `PICKUP_*` consts, `pickup` state, `updatePickup`/`syncPickup3D`/`buildPickupMesh`, `pickupMesh`, the `sfxPickup` sound, and the bot pickup-contest logic) is entirely deleted, and Seraphine's Astral Ward special no longer heals — it's now a stronger pure damage-reduction shield (200 frames). There is no way to regain HP anywhere in the game now (regen was already gone). (2) *No more hazards:* the environmental hazard system (`HAZARD_*` consts, `hazard` state, `pickHazardSpot`/`updateHazard`/`syncHazard3D`/`buildHazardMeshes`, the warn/active ground discs, `Fighter.takeHazardDamage`, the `sfxHazardWarn`/`sfxHazardActive` sounds, and the bot hazard-flee logic) is entirely deleted. The collapse-tick sound was renamed `sfxCollapseTick` (it had reused the hazard sound). The obsolete "Hazards & Pickups" tutorial card is replaced with a "No Healing" card. (3) *Draven's range:* confirmed — only his **special** (Ground Breaker) was halved (170→85); his **basic** attack range is unchanged at `75` (as the user expected). (4) *Collapse retimed:* the arena used to start closing ~9s in, which snatched the map away almost immediately. Added `GRACE_PERIOD` (75s) during which the arena stays full-size, then it ticks every `SHRINK_INTERVAL` (13s, up from 9) by `SHRINK_FRAC_STEP` (0.13). Crush now lands on the 7th tick at ~75 + 6·13 ≈ **153s (~2.5 min)** into a fully-stalled round — inside the requested 2-3 minute window (verified with `collapsecheck.js`). HUD reads "Arena collapses in Ns" during grace, then "Arena closing — next Ns".

**Converted to a first-person shooter, on request (big update).** This is the largest single change in the file's history — the game went from a fixed angled-camera 3D arena fighter (with a 2D-plane gameplay model underneath) to first-person. The underlying 2D-plane combat/movement math is unchanged; only the rendering, environment, and camera are new.
- *Cameras & split-screen:* the single reframing camera (`updateCamera`) is gone. Two `PerspectiveCamera`s (`camP1`/`camP2`, 72° FOV) sit at each fighter's eye (`EYE_HEIGHT` above their feet) looking along the fighter's 2D facing (`fx,fy` → world X,Z), slightly pitched down. `renderViews()` renders **split-screen** (left half = P1, right half = P2) when both sides are human, or **full-screen** from the human's eyes (or P1's, spectating a bot-vs-bot match) otherwise, via `setViewport`/`setScissor`. Rendering **layers** (`setLayerRecursive`) put each fighter's body on its own layer so a camera hides the *viewer's own* body (you don't see your own head from inside it) while still showing the opponent + environment; every scene light has `layers.enableAll()` so fighters on non-default layers are still lit.
- *Weapon viewmodels:* each player sees their own weapon (`buildViewmodel` → a small weapon prop + forearm stub, matching their `atkType`) held lower-right, attached as a child of their camera on a per-player viewmodel layer only their camera renders. `rebuildViewmodels()` rebuilds them each round; `syncViewmodels()` adds an idle bob + attack lunge. The cameras are added to the scene graph so their viewmodel children actually render.
- *Enclosed detailed environment:* the flat open floor became a real room — a tiled metal-deck floor (new tileable `buildFloorTexture` + `.repeat`), a very high ceiling (`CEILING_HEIGHT` 620, with emissive light strips + hanging lamps via `buildCeiling`), and four enclosing outer walls (`buildOuterWall`, ribbed `getWallTexture`) that **physically slide inward** with the collapse bounds (`syncOuterWalls`, called from `syncArenaCollapse`) instead of the old red boundary slabs. Pillars became floor-to-ceiling structural **columns**; platforms are now textured blocks (short = jump-on cover, tall = interior walls / lane dividers, same collision either way). Lighting upgraded (hemisphere fill + bigger shadow frustum).
- *Bigger, rebuilt maps:* the virtual space grew ~1.5x (`VIRTUAL_W/H` 1170×650 → 1755×975, aspect unchanged so the display container still matches; `ARENA_*` made symmetric, dropping the old top-down `HUD_HEIGHT` reservation). All six maps were **rebuilt from scratch** for FPS — enclosed arenas with columns, interior cover/dividing walls, jump-on platforms, and walkable steps/ramps, each with a different layout density so they feel different sizes. Verified with `geomcheck.js` (all spawns clear, no overlaps) and a new `fpscheck.js` that exercises the full FPS render path (`rebuildViewmodels`/`syncViewmodels`/`renderViews`) with real fighters without throwing.
- *HUD:* adapted for FPS — crosshair(s) centered in each active viewport, bigger character-colored HP + special bars scaled for the larger virtual space (P1 top-left, P2 top-right, naturally landing in each split-screen half), collapse timer + round banner centered. World-positioned floating damage numbers / hit-direction arrows (which only made sense in the top-down view) dropped.
- Tutorial gained a "First-Person View" card and the movement card updated, per the standing tutorial-sync rule.
- **Not visually verified** (no browser here): all of this is confirmed only for JS/runtime correctness (syntax + mock-DOM harness + geometry/spawn/render-path scripts). Proportions, art detail, viewmodel placement, camera pitch, split-screen framing, and overall look are reasoned first passes that will very likely need visual iteration.

**FPS turn controls, real muzzle origins, daytime look + art overhaul, and scenery, on request.** A follow-up polish pass on the first-person conversion:
- *Turn-to-look controls:* Left/Right (A/D and arrows) now **rotate** your facing (`turnBy`, `TURN_SPEED` rad/frame) like looking around, instead of snapping you to face sideways; Up/Down walk forward/back **along** your current facing. So you turn, then move that way — real FPS feel. The old movement block no longer calls `setFacing` for humans (only bots auto-face their movement + aim override); gamepad stick X = turn, stick Y = forward/back. Controls summary + the First-Person/Movement tutorial cards updated to match.
- *Shots leave the weapon:* ranged projectiles (basic + Lyra's volley) used to spawn at body-center/waist height; they now spawn at the **weapon muzzle** — out past the fighter's radius along its facing and up at `MUZZLE_HEIGHT` (34, hand height). Added a pooled world-space **muzzle flash** (bright puff + light, seen in both views) and a **viewmodel muzzle flash** at the weapon tip in the shooter's own view, both driven by a new `muzzleFlash` timer.
- *Daytime, bright, colorful:* replaced the black void with a **sky dome** (gradient blue zenith→pale horizon) + a soft **sun sprite**, matched by a strong warm directional **sun** light, a bright sky/ground **hemisphere** fill, and a cool bounce fill. Enabled **ACES filmic tone mapping + sRGB output** (exposure 1.1) and set sRGB encoding on the albedo textures, so colors read brighter and more saturated. Fog recolored to a light daytime haze and pushed far out; the indoor ceiling + ceiling lamps were removed — the arena is now an **open-air sunlit courtyard**.
- *Art overhaul:* the enclosing walls became **stone ramparts** with a coping cap and battlement merlons (open to the sky), the floor a warm **flagstone** tile, obstacles carved **sandstone columns** with plinths/capitals + a glowing accent orb, and interior cover/dividers **ashlar stone** blocks (new tileable stone floor + wall canvas textures replacing the old dark metal panels). Wall height dropped from a 620 ceiling to a 260 rampart so sky dominates above.
- *Scenery / extra obstacles:* new `buildScenery` scatters **braziers** (flickering flame + warm light, animated in `updateScenery`), **crates**, **barrels**, **urns**, and wall **banners** around each map. Crates/barrels/urns/braziers are **collidable** (registered as circular `OBSTACLES` + `mapEdgeObjects` so you fight around them and they fall away on collapse); placement is guarded by `nearSpawnPt`/`spotBlocked` so props never land on a spawn or clip existing geometry (`OBSTACLES` is now a per-load copy of the map's array so these pushes don't mutate map data). Verified by loading all six maps (scenery builds, obstacle counts rise, no throws) plus the usual syntax/harness/combat/geometry/render-path suite.
- Still **not visually verified** — brightness/exposure, stone texture detail, prop density and the sky framing are all reasoned first passes; expect to tune from screenshots.

**Bug fixed: fighter-select grid was empty (blank character-select screen).** Root cause: the daytime/FPS lighting + sky + layer code calls `Object3D.layers.enableAll()` / `disableAll()` all over (cameras, lights, sky dome, sun, per-fighter body layers) — but those two `Layers` methods were **only added to three.js in r130**, and this game pins **r0.128.0** (the CDN `<script>`). So `makeFpsCamera`'s `cam.layers.disableAll()` threw `TypeError` at load, on line ~1197 — *before* the top-level `buildGrid('p1-grid', …)` calls (line ~4178) that populate the fighter cards. The static menu HTML (panels, VS, buttons) still rendered, but the JS-built grid stayed empty. The mock-DOM harness never caught it because it turned every method — including nonexistent ones — into a callable mock. Fix: a tiny **polyfill** right after the scene is created that defines `THREE.Layers.prototype.enableAll/disableAll` only if missing (so a future THREE upgrade is unaffected), leaving all 13 call sites unchanged. Validated the mask math directly (camera hides its own body / sees the opponent / lights reach all bodies). Also **hardened `mockharness.js`**: it now hands out a real r128-accurate `Layers` instance for every object's `.layers` (with `set/enable/disable/test` but *no* `enableAll/disableAll`), so using any newer-than-r128 Layers API now throws in the harness instead of being silently swallowed — confirmed it catches this exact bug when the polyfill is removed.

**Bug fixed (second cause of the blank character-select): `getWallTexture` fed an `undefined` color to `addColorStop`.** After the Layers polyfill, the menu was *still* blank — the DevTools console showed `Uncaught SyntaxError: Failed to execute 'addColorStop' on 'CanvasGradient': The value provided ('undefined') could not be parsed as a color` at `getWallTexture → buildOuterWall → loadMap` (the top-level `loadMap(MAPS[...])`), again dying before `buildGrid()`. Root cause: the ashlar-stone texture's block loop starts at `x = -blockW` to tile a half-offset row, so `Math.floor(x / blockW)` is `-1`; `(row*3 + -1) % tints.length` is **negative** for `row = 0` (JS `%` keeps the sign), and `tints[-1]` is `undefined` — which `fillStyle` would silently ignore but **`addColorStop` throws on**. Fixed with a non-negative modulo `((n % m) + m) % m`. The all-mock harness missed this because its `addColorStop` was a no-op; **built a much more faithful checker** (`realthree_check.js`) that runs the page against the *real* downloaded `three@0.128.0` build (stubbing only WebGL/DOM/Audio) **and** validates gradient colors exactly like a browser (throws on unparseable) — it reproduced the bug and now passes. Folded the same browser-accurate `addColorStop` color validation into `mockharness.js` so the persistent harness catches invalid/`undefined`/`NaN` gradient colors going forward. **Lesson:** canvas `addColorStop`/`createPattern` are the few 2D-canvas calls that *throw* on bad input (unlike `fillStyle`), so a no-op canvas mock can't catch a whole class of load-time-fatal bugs.

**Each map now has a distinct visual identity (theme), on request.** All six arenas used to share one sandstone-castle look with only the accent color changing; now each is instantly recognizable. Added a `MAP_THEMES` table (keyed by map name) + `themeFor(map)`, and made every environment builder theme-driven:
- **Astral Proving Grounds** — cyan energy arena: dark steel walls with glowing accent seams + a bright top rail, tech-grid floor, sleek metal **pylon** columns (glowing rings), glowing pylon/crate scenery, clear cool vivid-blue midday.
- **Sundered Stair** — desert temple: warm sandstone block walls with **stepped ziggurat** tops, rippled sand floor, sandstone columns, urns/braziers, golden-hour hazy sky + warm low sun.
- **Twin Ramparts** — grey granite **castle**: classic battlement merlons, grey flagstone floor, grey stone columns, crates/barrels/banners, soft cool overcast-but-daytime light.
- **The Colosseum Ring** — cream **marble** + grass: smooth rounded coping with draped **ivy**, grassy floor with dirt patches, fluted marble columns, bushes/urns, brightest clear midday.
- **Skyreach Spire** — red-rock **canyon**: rough **jagged** cliff tops, red strata walls, red-sand floor, stacked-rock **spire** columns (hoodoos), boulders/rocks/bushes, warm dusty afternoon light.
- **The Shattered Bridge** — frozen **glacier**: pale cracked-ice walls with angular **ice-spike** tops, icy floor, ice-**crystal** columns, ice-shard scenery, crisp cold pale-blue light.

Implementation: theme-aware `getWallTexture`/`getFloorTexture` (new canvas generators for metal/sandstone/ashlar/marble/redrock/ice walls and techgrid/sand/flagstone/grass/redsand/ice floors, cached by theme id), `buildOuterWall` switches the top silhouette by `wallStyle` (battlement/techrail/ziggurat/ivy/jagged/crystal), `buildPillarMesh` switches column form by `column` (stone/pylon/marble/redspire/icecrystal), steps/ramps/platforms take the theme's stone/wall colors, and the **sky dome + sun + directional/hemisphere/ambient lights + fog are recolored per map** by `applySky`/`applyLighting` (all daytime — cool clear midday through warm golden hour, never night). New scenery props (bush, boulder, rock, ice shard, tech pylon) selected per theme. All still confirmed via the real-three r128 harness loading **all six themed maps** with no throw, plus geometry/spawn/combat/render-path checks — but the actual look (palette balance, texture detail, brightness) is unverified and may want tuning from screenshots.

**Four more maps + themes (10 total, one per fighter), on request.** Added four brand-new themes/maps, each visually distinct from the six above and each other: **Molten Foundry** (volcanic — black basalt walls with glowing lava seams, jagged tops, lava-crack floor, basalt spires, smoky warm daylight), **Overgrown Sanctuary** (jungle — mossy grey-green ruin walls with vine drapes, jungle-litter floor, stone idols, humid green light), **Skyward Temple** (celestial — white marble + gold coping/finials, gold-inlay floor, gilded columns, bright pastel dawn), and **Coral Hollow** (coral — pink coral-stone walls with coral spikes, turquoise coral-sand floor, coral-crystal columns, bright tropical light). Implementation reused the theme system: added `basalt/mossstone/cloudmarble/coralstone` wall textures + `lava/jungle/cloud/coralsand` floor textures, a new `gilded` wall style, made the `crystal` wall spikes / `icecrystal` columns / ice-shard scenery take their color from the theme (so the same silhouette reads as ice OR coral), and per-theme scenery sets. Also **renamed "Astral Proving Grounds" → "Voltaic Nexus"** on request. Verified all **ten** themed maps load under real three r128 (geometry/spawns/scenery) + combat/render-path checks.

**Map-select screen + drop-in intro cinematic, on request.** Two new flow features:
- *Map select:* after both fighters lock in, `beginMatch()` now opens a **Choose Your Arena** screen (`#mapselect-screen`) instead of jumping straight in — a 5-wide grid of **hand-painted thumbnails**, one per map (`buildMapThumbnail` renders each theme's sky gradient + sun glow + wall silhouette with its actual top-shape + columns + floor + accent + vignette), plus a prominent **🎲 Random Arena** button (the default) and a Back-to-Fighters button. Clicking a card or Random starts the match on that map. Rematch buttons still bypass it.
- *Drop-in intro:* a new `INTRO` game state. `startRound(true)` (match start only, not between rounds) perches both fighters at `INTRO_DROP_HEIGHT` above spawn facing each other; the INTRO loop branch drops them in under gravity — **in first person each player's own view plummets from the sky into the arena** (camera tilts steeply down during the fall). On landing: a camera jolt, a shockwave ring, and a heavy thud; then a beat, then a scaling **"FIGHT!"** flash and control unlocks. `drawIntroOverlay` renders a cinematic overlay (letterbox bars, glowing arena name, the `P1 VS P2` matchup, and the FIGHT!/GET READY text). Verified: thumbnails build for all 10 maps under the color-validating real-three harness, and the intro state machine loads/loops without throwing. **Not visually verified** — the drop timing, camera pitch, thumbnail art, and FIGHT! styling are reasoned first passes.

**First-person viewmodel attack animations made distinct; Nyx/Thorne weapons lengthened to match range, on request.** (1) *Viewmodel animations:* the weapon in your own hands used to do one generic forward/back "lunge" for every attack. Now `syncViewmodels` drives a **distinct motion per weapon type** (`viewmodelAnimKind`): swords/scythe/whip **slash diagonally across the view** (roll+yaw+pitch swept by `swingT` from wind-up to follow-through), daggers **double-thrust**, hammers **raise then slam overhead**, fists **piston in and out** (two jabs), and casters **recoil** (kick back + muzzle-up as the shot leaves). All are driven off the real startup/active/recovery frame windows so they sync with the actual hit. (2) *Weapon length ≈ range:* the fighter mesh is 1:1 with game units (no base scale), so a weapon's local length maps straight to range units — but Nyx's scythe (~42) and Thorne's whip (~28) were far shorter than their ranges (95 / 115), so the visible reach didn't match the slash area. `buildScytheProp(len)` and `buildWhipProp(n, step)` are now length-parametrized; **Nyx's on-body scythe is built at len 60** and **Thorne's whip at 13 segments × 6.5** so the blade/lash reaches (close to) the edge of their attack range during the swing. The first-person viewmodel versions are kept shorter/foreshortened (scythe 46, whip 9×4) so they read as long-reach without filling the screen. Verified via real-three r128 (all fighters build) + combat + render-path checks; exact reach=range matching is a reasoned first pass to tune from screenshots.

**Caster attack magic + distinct, elaborate special animations, on request.** (1) *Caster magic:* the three casters (Lyra shard, Seraphine orb, Aurelia bolt) now get a **magical charge glow** — an additive emissive orb at the weapon core (rig) and at the weapon tip (viewmodel) that swells + brightens through the cast wind-up, flickers on release, and fades in recovery (`_castCharge` for the rig, a `charge` mesh in the viewmodel), on top of the existing release muzzle flash. (2) *Special animations:* specials used to reuse the basic `weaponAnim`; now each of the ten has its **own, more elaborate motion** for both the third-person rig and the first-person viewmodel, keyed by the character's special id via a new `SPECIAL_ANIMS` table + a `specialAnim` on the mesh (used by `animateWeapon` when `atkKind === 'special'`). The rig animator gained wind/strike/poseGain phase math and new motion types: Kaelen **dash** (draw back → explosive forward lunge that drives the body forward), Gorgonok/Draven **slam** (hoist overhead → crash down with a body drop, Draven's bigger), Nyx **reap** (full spinning scythe sweep + hop), Voss **blinkflurry** (blink-in + rapid alternating stab storm), Aurelia **skybolt** (point to the sky, then snap down + weapon flash), Ignis **flameburst** (crouch/draw-in → burst forward + pop up), Lyra **volley** (raised sweeping multi-pulse cast), Seraphine **ward** (raised, slowly-spinning glowing guard), Thorne **whipsweep** (huge wide body-twisting lash). The viewmodel has matching per-special first-person motions. **Bug avoided:** the new per-frame weapon-scale reset had to be guarded (`ws = weapon && weapon.scale ? weapon : null`) because dual-wielders (Ignis, Voss) store `weapon` as an **array** — caught and fixed via a new real-three harness pass that drives every character's basic+special animation through all three states. **Not visually verified** — amplitudes/timing are reasoned first passes to tune from screenshots.

---

## Read-this-before-you-touch-anything (comment-truth audit)

- [x] Line-235 delta-time comment vs `computeDt()` never called — **fixed**, `computeDt()` is now called every frame in `gameLoop` and `dt` is threaded through `update`, `botDecide`, `tickEffects`.
- [x] `hitStopFrames` declared but never read/written — **fixed**, set (scaled by damage) in `takeDamage()`, read/decremented at the top of `gameLoop` to freeze physics for a beat.
- [x] `pollGamepad()` called but doesn't exist — **fixed**, implemented and called from both the P1 and P2 input branches in `update()`.
- [x] `steerAroundObstacles()` called but doesn't exist — **fixed**, implemented with the exact call signature `steerAroundObstacles(cx, cy, moveX, moveY)` → `{steerX, steerY}`.
- [x] `this.commitTimer` read but never initialized/assigned — **fixed**, initialized to `0` in the constructor and set to `24` by `useSpecial()`.

---

## Batch 0 — Make it run

- [x] **`pollGamepad` undefined at the two call sites** — implemented against `navigator.getGamepads()`. Returns `{moveX, moveY, jump, attack, special, skill}`.
  - `[~]` Spec asked for **deadzone ~0.25**; current implementation uses **0.2**. Close but not exact — flag for a follow-up tweak.
  - `[ ]` Spec asked for **edge-triggered buttons**; current implementation reads `.pressed` every frame (held-state), same as the keyboard's `keys` map. This means gamepad attack/special/skill currently have the same "fires every frame while held" problem Batch 3a calls out for keyboard — not yet edge-triggered. Will be resolved together with Batch 3a's `pressed` set.
- [x] **`steerAroundObstacles` undefined at its call site** — implemented, matches the fixed signature.
- [x] **`computeDt()` never called, `dt` undefined everywhere** — wired into `gameLoop`; threaded through `update(opponent, dt)`, `botDecide(opponent, dt)`, `tickEffects(opponent, dt)`. Landing test (`nextZ <= landHeight`) now receives real numbers instead of `NaN`, so jump/land works.
- [x] **`this.commitTimer` never initialized** — initialized in constructor; commitment window implemented (`useSpecial()` sets it to 24 frames-equivalent, movement speed multiplier drops to 0.4 while `attackCooldown > 0 || commitTimer > 0`).

**Verify (self-assessed, not yet re-run after this checklist was written):**
- [ ] Two fighters, 30s fight, zero console errors
- [ ] Jump and land confirmed
- [ ] Bot moves and attacks confirmed

---

## Batch 1 — Make time mean the same thing everywhere

Six `dt`-scaled expressions existed before the last update; the rest of the list below was converted in the same pass. Two spots were missed:

- [x] `q.delay--` → `processQueue` — now `-= dt`
- [x] `attackCooldown--` → `update` — now `-= dt`
- [x] `skill2Cooldown--` → `update` — now `-= dt`
- [x] `shieldFrames--` → `update` — now `-= dt`
- [x] `hp += regen` → `update` — now `+= regen * dt`
- [x] `specialMeter += 0.05` (Gorgonok passive) → `update` — now `+= 0.05 * dt`
- [x] `hitFlash--` → `tickEffects` — now `-= dt`
- [x] `invulnFrames--` → `tickEffects` — now `-= dt`
- [x] `e.r += 8` (shockwave expansion) → `tickEffects` — now `+= 8 * dt`
- [x] `e.life--` → `tickEffects` — now `-= dt`
- [x] `p.x += p.vx / p.y += p.vy` (projectiles) → `tickEffects` — now `* dt`
- [x] `hazard.timer++` → `updateHazard` — now `+= dt`
- [x] `pickup.timer++` → `updatePickup` — now `+= dt`
- [x] `arenaTime++` → `gameLoop` — now `+= dt`
- [x] `deathTimer++` → `gameLoop` — now `+= dt`
- [x] `p.vy -= 0.15` (renderDeathAnim) → now `-= 0.15 * dt`
- [x] Death particle position (renderDeathAnim) → now `* dt`
- [x] `pickupMesh.rotation.y += 0.02` (`syncPickup3D`) — **fixed**, now `+= 0.02 * dt`; `syncPickup3D` takes `dt` as a param and both call sites in `gameLoop` pass it.
- [x] `orbiter.rotation.z += 0.03` (`render3D`, Seraphine halo spin) — **fixed**, now `+= 0.03 * dt` (the other orbit mode, Lyra's shard, already rode on `arenaTime` which was already dt-scaled, so it needed no change).

- [~] Fractional-frame-counter check: spot-checked `Math.ceil(f.skill2Cooldown / 60)` display math and the hazard/pickup `>=` state-transition comparisons — both tolerate fractional `dt` values fine (no exact-equality comparisons found). Not exhaustively audited beyond that.

**Batch 1 status: complete**, modulo the un-run verify pass below and the two Batch-0 gamepad spec deviations noted above (deadzone 0.2 vs 0.25, held- vs edge-triggered buttons — deferred to Batch 3a since keyboard has the identical issue).

**Verify:**
- [ ] 30fps-capped vs 60fps vs uncapped wall-clock parity — not yet tested (no in-game FPS cap debug flag exists yet; that's Batch 0's `?debug=1`, not yet added)

---

## Batch 2 — Correctness bugs

- [x] 1. `endGame()` ignores computed `winner`/`loser`, re-derives from `player1.hp <= 0` — **fixed.** `endGame()` now reads `winner`/`isDraw` directly, never re-derives from live `hp`. Comment added explaining why (a still-in-flight projectile can drop the original winner to 0 hp during the death sequence).
- [x] 2. No draw on double-KO — **fixed.** New `isDraw` flag + `deathCandidates` array (replaces the single `loser` reference for the death-sequence animation, since a draw needs to animate both fighters dying, not one). `endGame()` shows "Draw!" when set.
- [x] 3. Teleports/knockback skip collision — **fixed.** New `Fighter.wouldCollide(nx, ny)` + `Fighter.teleportTo(dx, dy)` (steps in ~8-unit increments, stops at the first pillar/platform it would cross, then `clampToArena()`). Replaced the raw `this.x += this.fx * N` in `dash`, `flurry`, `blink`, `step`, and `pound`'s `opponent.x += ...` knockback with `teleportTo(...)` calls.
- [x] 4. Hardcoded spawns regardless of map — **fixed.** Each of the 4 `MAPS` entries now has `spawn1`/`spawn2`; `startFight()` reads them instead of the old literal `200,300`/`650,300`. Sundered Stair and Twin Ramparts got new spawn coordinates (their old defaults genuinely did overlap a step/platform, confirmed by hand before fixing). Added `assertSpawnsClear(map)`, called from `loadMap()`, which `console.warn`s (doesn't throw) if a spawn overlaps a pillar or platform — a dev-time net for future map edits, not a runtime fix.
- [x] 5. Height not enforced on projectile hits / `inMeleeCone` — **fixed.** New `verticalHit(attackerZ, defenderZ)` helper (36-unit hurtbox + 10-unit tolerance) gates both the projectile-vs-opponent AABB test and the top of `inMeleeCone`. Jumping high enough now genuinely dodges ground-level melee and projectiles.
- [x] 6. `inMeleeCone` ignores defender's radius — **fixed.** Reach is now `range + defender.width / 2` instead of bare `range`.
- [x] 7. Rematch re-randomizes the map — **fixed.** `startFight()` now takes an optional `mapOverride` param. "Rematch (Same Map)" passes `currentMap`; added a second button "Rematch (New Map)" that calls `startFight()` with no arg for an explicit reroll.
- [x] 8. Hazard damage strobes `hitFlash` and is blocked by i-frames — **fixed.** New `Fighter.takeHazardDamage(amount)` — applies shield reduction but skips `invulnFrames` entirely and never touches `hitFlash`. `updateHazard()` calls it instead of `takeDamage()`.
- [x] 9. `pickHazardSpot` only avoids `OBSTACLES` — **fixed.** Now also checks `PLATFORMS`, `TERRAIN`, `PICKUP_SPOTS`, and both fighters' current positions.
- [x] 10. Dead code / stale comment — **fixed.** `shade()` and `hexToRgb()` removed (confirmed zero other call sites before deleting). `gameState` comment now reads `// MENU, FIGHT, DEATH, GAMEOVER`.
- [x] 11. Only Space gets `preventDefault()` — **fixed.** Now also covers all four arrow keys and `/`.

**Batch 2 status: complete.** Syntax-checked and smoke-opened in browser; the "no console errors across a full match" and "ten rematches" verify-list items still need an actual play session, not just a syntax check.

---

## Batch 3 — Combat feel

- [x] 3a. Edge-triggered input — **fixed.** New `pressed` Set (populated only on the keydown down-transition, cleared on `startFight()`) + `consumePress(k)`. `pollGamepad` now tracks per-button previous state and returns `justPressed`, closing the two Batch-0 gamepad deviations too (deadzone raised to the spec'd 0.25; buttons now edge-triggered).
- [x] 3b. Startup/active/recovery — **fixed.** New `FRAME_DATA` table (per character, per `basic`/`special`/`skill`, tunable in one place). `attack()`/`useSpecial()`/`useSkill2()` renamed to `doBasicAttack`/`doSpecial`/`doSkill` and now only fire at the moment "active" begins; `tryStartAction(kind, opponent)` gates entry and `updateActionState(dt)` advances the state machine. Draven's hammer basic is 16/6/24 (genuinely committal); Voss's dagger is 3/3/5 (near-instant). Surfaced in the `?debug=1` overlay per-fighter.
- [x] 3c. Hitstun + resolved knockback + wired hit-stop — **fixed.** New `hitstunTimer` (scales with damage, blocks acting and movement) set in `takeDamage()`. Knockback now goes through `teleportTo()` (sweeps and stops at the first pillar/platform) instead of a raw position add. Getting hit also cancels whatever action state you were in — no trading a slow recovery for a free counter-hit.
- [x] 3d. Defensive option — **added dodge roll** (brief's own stated preference). `tryDodge()`: short i-framed (`invulnFrames`) dash in the held movement direction (or current facing if not moving), on a cooldown. P1: Ctrl. P2: `.` (period).
- [x] 3e. Make jump worth pressing — covered by existing/adjacent mechanics rather than a bespoke new system: height-based dodging already works (Batch 2 item 5), attacks were never grounded-only so air attacks already work, and the new dodge roll works airborne too (`teleportTo` doesn't check `grounded`), functioning as an omnidirectional air-dash. No dedicated new air-only mechanic was added.
- [x] 3f. Aim-without-walking modifier — **added.** Holding the aim-lock key (P1: Shift, P2: `,`) still applies movement velocity but skips the `setFacing()` call, so you can walk one way while facing/attacking another.

**Batch 3 status: complete.** This was the batch the brief called out as deciding whether the game is fun — biggest change of the whole pass. Verified: syntax-check clean, and a full top-to-bottom mock-DOM execution (catches `ReferenceError`s that only show up at runtime, the same class of bug as the `camera` regression) completes without throwing. Not yet verified: actual play-feel tuning of the `FRAME_DATA` numbers, which are a reasoned first pass, not playtested.

---

## Batch 4 — Balance

- [x] 1. Retune HP/DPS for 20-40s TTK — **fixed.** HP raised ~4x (100-130 -> 320-520), basic-attack damage roughly halved. Combined with Batch 3b's frame-data windows (which already cut raw DPS 25-35% on their own), worst-case time-to-kill now lands at ~20-28s per character — full math is in a comment above `FRAME_DATA`. Reasoned, not playtested.
- [x] 2. Draven's Bulwark Stance uptime — **fixed.** 240/260 frames (~92% uptime) -> 90/220 (~41%).
- [x] 3. Unify special-meter generation — **fixed.** New `METER_DEALT_RATE`/`METER_TAKEN_RATE`/`METER_PASSIVE_RATE` constants + `meterRateMult` per character in `CHARACTERS`. Meter now comes from `takeDamage()` (both the attacker dealing it and the defender taking it — a comeback mechanic), not from firing-whether-or-not-it-hit. Gorgonok's `if (this.name === 'Gorgonok')` passive-charge special case is gone; a generic passive trickle now applies to everyone, scaled by `meterRateMult` (his is 1.8, highest, preserving his identity without a name-check).
- [x] 4. Retune `PICKUP_HEAL_PCT` — **fixed.** 0.22 -> 0.14, and `PICKUP_INTERVAL` 280 -> 500 frames (fights are now ~10x longer, so the old interval could spawn a heal ~6 times in one match).
- [x] 5. Cap/condition Seraphine's sustain — **fixed.** Passive `regen` 0.05 -> 0.035 (still the roster's highest). Astral Ward's heal and Radiant Pulse's heal were both flat numbers that became trivial once HP pools got 4x bigger — converted to 8%/10% of her own maxHp instead.
- [x] 6. Explicit `range`/`halfAngle` for `shard`/`orb` — **fixed.** Both Lyra and Seraphine now have `range: 260, halfAngle: 15 * DEG` in config instead of relying on the `cfg.range || 0` fallback. (Functionally inert for their own basic attack, which fires a projectile rather than using `inMeleeCone` — this was purely a config-completeness fix, as the brief asked for.)

**Batch 4 status: complete.** Syntax-checked and passed a full mock-DOM execution with no runtime errors. The actual numbers are a reasoned first pass from the frame-data math, not a playtested result — real balance needs actual matches, which I can't run.

---

## Batch 5 — Match structure

- [x] Round timer + HP% decision + draw on tie — **fixed.** `ROUND_TIME = 60`s, visible countdown in the HUD (turns red under 10s). `decideRoundResult(byTimeout)` handles both the KO path and the timeout path; timeout compares `hp/maxHp` (not raw hp, since maxHp differs per character), draws if within 0.1%.
- [x] Best-of-three + pips + inter-round reset — **fixed.** `ROUNDS_TO_WIN = 2`. New `ROUND_END` game state shows a "Round N: X wins!" banner + updated pips for 90 frames, then `startRound()` resets both fighters (same map, same score) automatically. `startFight()` split into `startMatch()` (resets score/stats/map, call this for a genuinely new match) and `startRound()` (resets only the fighters, called between rounds).
- [x] Match summary — **fixed.** New `matchStats` (`damageDealt`, `largestHit`, `specialsUsed` per player, updated live from `takeDamage()`/`doSpecial()`) + `roundDurations` (wall-clock per round). Rendered into `#match-summary` on `endMatch()`.
- [x] Pause (Escape) — **fixed.** New `PAUSED` state; Escape toggles it from `FIGHT`/`DEATH`/`ROUND_END`, freezing the update loop entirely (still renders the frozen frame). Resume and Return-to-Menu buttons on a new pause screen, which also carries the full control list (partially covers Batch 9 item 8 too).
- [x] Skippable death sequence — **fixed.** After `DEATH_SKIP_AFTER` (15 frames), any of F/K/Space/Enter (edge-triggered via `consumePress`) jumps straight to `endRound()`.

**Batch 5 status: complete.** Syntax-checked and passed the mock-DOM execution. This was a substantial rewrite of the win/loss flow (`endGame` split into `endRound`/`endMatch`, `startFight` split into `startMatch`/`startRound`) — every call site was checked and updated, but this is exactly the kind of change that most needs an actual play session to confirm the round-to-round transitions feel right, which I haven't been able to run.

---

## Batch 6 — The bot

- [x] Difficulty tiers — **fixed.** New `BOT_DIFFICULTY` table (easy/normal/hard), each varying `thinkMin`/`thinkMax` (reaction gap), `atkChance`/`specialChance`/`skillChance`, and two new behavior flags (`punishRecovery`, `dodgeProjectiles`). Cycled via a menu button.
- [x] Bot contests the heal pickup — **fixed.** Weighted by `1 - hp/maxHp` (missing HP fraction) — a near-full bot won't detour for a sliver, a low-HP one will.
- [x] Bot reacts to hazard `warn` — **fixed.** Avoidance check now fires on `warn` (smaller radius, since exact activation is still unknown) or `active` (full radius), not just `active`.
- [x] Purposeful jumping — **fixed.** Bot now jumps when its intended movement direction is about to run into a platform wall (checked ~40 units ahead), not a pure random twitch — the old `0.002*dt` twitch is kept only as a small residual "look alive" fallback.
- [x] Bot dodges projectiles — **fixed.** Scans the opponent's in-flight projectiles; if one is within 90 units and this difficulty has `dodgeProjectiles`, calls `tryDodge()` perpendicular to the projectile's velocity.
- [x] Difficulty selection, bot-vs-bot, bot option for P1 — **fixed.** New `btn-toggle-bot-p1` mirrors the existing P2 toggle (both independent, so both-on gives bot-vs-bot for free); `btn-difficulty` cycles easy/normal/hard.
- [x] Bot respects the startup/active/recovery state machine — **fixed as a byproduct of Batch 3**, since the bot's action calls already went through `tryStartAction` (which gates on `atkState`/`hitstunTimer`) once that batch landed. Batch 6 additionally makes `hard` difficulty actively punish an opponent's `recovery` window (shortens the think gap and boosts attack chance 1.6x the instant it's detected) rather than just passively not-attacking during its own.

**Batch 6 status: complete.** Syntax-checked and passed the mock-DOM execution.

---

## Batch 7 — Performance and memory

- [x] Proper disposal walk (`traverse` → dispose geometry/material/maps) from `clearMapGroup`, `Fighter.disposeMesh`, every `scene.remove` site — **fixed.** New `disposeObject3D(obj)` (traverses, disposes geometry + material + material.map) and `removeAndDispose(obj)` (= `scene.remove` + dispose), declared early in the file (right before the "3D arena" builder section) so they're initialized before any startup-time mesh building can reach them — the same TDZ-ordering lesson as the `camera` regression documented above. Every prior bare `scene.remove(x)` call site (`tickEffects` effect/projectile cleanup, `Fighter.disposeMesh`, `renderDeathAnim` death-particle cleanup, low-HP-particle cleanup, `clearMapGroup`) now goes through `removeAndDispose`.
- [x] Cache the four floor textures once at startup instead of rebuilding per fight — **fixed.** `getFloorTexture(map, accent)` caches by `map.name` in `floorTextureCache`; `loadMap()` calls it instead of `buildFloorTexture(accent)` directly. Since rounds (Batch 5) reload the same map 2-3x per match, the 512x512 canvas texture is now drawn once per map name for the life of the page, not once per round. The floor's material is flagged `userData.keepMap = true` so `disposeObject3D`'s per-round `clearMapGroup` walk doesn't free the shared texture out from under the cache.
- [~] Pool effect meshes instead of allocating a `RingGeometry` + material per swing — **addressed via geometry sharing, not literal pooling.** `getCachedGeometry(key, factory)` caches `RingGeometry`/`BoxGeometry` shapes by their shape parameters (radius/segments/arc angle), so identical swing shapes (which is most of them — a character's basic attack always sweeps the same arc) now reuse one GPU buffer instead of allocating a new one per swing. Materials stay per-instance (each effect fades its own `opacity` independently every frame — sharing a material would make one fading slash dim every other on-screen effect using that material simultaneously, which would be a visible bug, not an optimization). The `streak` effect type (variable-length drag line) was left uncached — its length is different almost every time, so a geometry cache for it would just grow unbounded for no reuse benefit.
- [~] Pool projectile meshes and death particles — **addressed via geometry sharing, not literal pooling**, same reasoning as above: `projectileSphere`, `deathParticleSphere`, and `lowHpPuffSphere` are now cached geometries (all instances of each are literally identical spheres), materials stay per-instance for independent opacity/color fade.
- [x] Share geometry/materials across identical objects (pillars, daggers, death particles) — **fixed for pillars and particles** (see above; pillar base/crystal geometry *and* material are now cached per-map since pillars never animate opacity individually — `getCachedMaterial` added alongside `getCachedGeometry` for this reason). **Daggers were already shared** before this batch: Voss's second dagger is built via `dagger1.clone()`, and Three.js's `Object3D.clone()`/`Mesh.copy()` shares the geometry and material by reference rather than deep-copying them, so that requirement was already satisfied incidentally.
- [x] FPS / draw-call counter in debug overlay — **fixed.** The existing FPS/dt/hitstop line in `drawDebugOverlay()` now also reads `renderer.info.render.calls` and `renderer.info.render.triangles` (WebGLRenderer's built-in per-frame counters, reset automatically each `render()` call).

**Batch 7 status: complete**, with two items scoped down from literal object-pooling to shared-geometry caching (see notes) — this gets the actual perf/memory win (no more per-swing/per-particle GPU buffer allocation) with far less risk than a full pool/lifecycle rewrite, which the brief's own ground rules ("don't refactor for its own sake") argue against absent a measured need for it. Syntax-checked and passed the mock-DOM execution; caught and fixed one real bug in the process — `geometryCache`/`materialCache`/`disposeObject3D` were originally declared near their Batch-7-era call sites (~line 820), but `buildPillarMesh()` runs at startup (via the initial `loadMap()` call) before the script reaches that point, throwing the exact same class of TDZ `ReferenceError` as the earlier `camera` regression. Moved the whole block earlier in the file, before the "3D arena" builder section, to fix it — confirmed via the mock harness before and after.

---

## Batch 8 — Visuals

- [x] `renderer.setPixelRatio()` (clamped ~2) + fix `canvas.width` being set from CSS pixels via `getBoundingClientRect()` instead of device pixels — **fixed.** New `PIXEL_RATIO = min(devicePixelRatio, 2)` constant, `renderer.setPixelRatio(PIXEL_RATIO)` set once at renderer creation. `onWindowResize()` no longer manually stomps `canvas.width/height` (that fought `renderer.setSize`, which already multiplies by the configured pixel ratio internally) — it now only manually sizes `hudCanvas` (a plain 2D canvas with no such helper) by `w/h * PIXEL_RATIO`. The CSS `canvas { width: 100%; height: 100% }` rule (not the width/height attribute) governs on-page layout size either way, so this only changes sharpness, not layout.
- [x] Fighter animation (wind-up lean, weapon sweep, recovery off-balance) driven by Batch 3b's frame windows — **fixed.** `render3D` now computes a whole-mesh forward/backward pitch (`this.mesh.rotation.x`) from `atkState`/`atkStateTimer` against `FRAME_DATA`: forward dip through `startup`, sharper forward lunge right as `active` begins (easing off through the active window), backward off-balance wobble through `recovery` settling back to neutral. Deliberately whole-mesh rather than per-weapon-part swing — six very different character rigs (box body, octahedron shard, sphere+halo, etc.) share one generic animation instead of six bespoke riggings, in keeping with "don't refactor/redesign beyond what's asked."
- [x] Richer hit feedback: impact spark, damage-scaled camera kick, floating damage numbers, white flash on victim material — **fixed.** New `damageNumbers`/`impactSparks` arrays (plain 2D HUD-space entries, not THREE meshes — drawn via `drawHitFeedback()` in the existing virtual-space HUD pass) populated from `takeDamage()`'s attacker branch: a floating number that drifts up and fades, and an expanding/fading ring at the hit point tinted the attacker's color. `takeDamage` already set `hitFlash` on every real hit; it just never drove anything beyond the `flashLight` point light. `render3D` now also lerps the victim's body materials toward white by `hitFlash`'s decay (distinct from the i-frame flash, which stays a *solid*, sustained white so the two read differently). Camera kick already existed (`cameraShakeMag`, scaled by damage) — untouched.
- [x] Ability-readiness indicator at the fighter's feet (special/skill, not just the existing basic-attack `cooldownRing`) — **fixed.** Two small ground pips flank the existing `cooldownRing`: `specialReadyRing` (cyan, lights and gently pulses once `specialMeter >= 100`) and `skillReadyRing` (violet, same once `skill2Cooldown <= 0`). Both dim (opacity 0.12) while charging.
- [x] `shieldRing` tinted per source instead of hardcoded yellow — **fixed.** New `this.shieldColor` field, set alongside `shieldFrames` at each of the two shield-granting call sites (Seraphine's Astral Ward: yellow `0xfacc15`; Draven's Bulwark Stance: slate `0xcbd5e1`, matching each skill's existing 2D effect-ring color). `render3D` applies it to `shieldRing.material.color` while the shield is up.
- [x] Fog near/far tuning so arena edges aren't permanently hazy — **fixed.** `Fog(0x0a0d14, 500, 1400)` → `Fog(0x0a0d14, 900, 2000)`. The camera sits ~690-810 units from arena center/far-corner at its default framing, so a 500-unit near start meant the entire playable floor was already inside the falloff; near is now past the arena's farthest corner so fog only eats the empty background beyond the fight.
- [x] Mirror-match readability (per-side tint shift, distinct ground ring color, name tag) — **fixed.** P2's body materials are darkened a notch (`offsetHSL(0,0,-0.14)`) at mesh-build time, with `origColor` updated to match so the hit-flash/i-frame-white restore paths revert to the *tinted* shade rather than briefly "un-tinting" P2 on every hit. New floating `makeNameSprite('P1'/'P2', ...)` canvas-texture sprite above each fighter's head (cyan/pink, `depthTest: false` so it stays legible behind pillars or the opponent). The pre-existing P2-only ground ring is unchanged.
- [x] HUD: character-colored bars, numeric HP, damage-ghost trail, per-round pips — **fixed** (pips already existed from Batch 5). HP bar fill color is now `f.color` (falls back to the existing red flash under 25% HP) instead of a flat hardcoded green. Numeric `current/max` HP text added over the bar. New `hpGhost` field per fighter (decays toward `hp` at a fixed rate in `render3D`, snaps up instantly on heals) drawn as a lighter bar underneath the real one — the classic fading "you just lost this much" afterimage.
- [x] Off-camera hit-direction indicator — **implemented as an in-HUD-panel indicator, not literally screen-edge.** The camera is a fixed, arena-framing angle (not a follow-cam that can lose a fighter off-screen), so a true "off-camera" edge indicator doesn't apply the way it would in a shooter; the brief's underlying intent — know which way a hit came from without parsing the 3D shake — is covered instead by a small arrow on each fighter's own HUD panel (`f.hitDirAngle`/`hitDirTimer`, set in `takeDamage`), pointing at the attacker's relative 2D position and fading over 30 frames.
- [x] Outline/rim pass on fighters — **fixed via inverted-hull technique, not a post-processing pass.** A true screen-space outline needs `EffectComposer`/`OutlinePass`, which aren't part of the core `three.min.js` build the brief pinned via jsDelivr — pulling those in would mean a second external module source, against the brief's own "Three.js stays the one external dependency" rule. `addRimOutline(group, colorHex)` instead adds a slightly-scaled (1.08x), `BackSide`-material clone behind every shadow-casting mesh in a fighter's group (a good free proxy for "real body/weapon part" vs. rings/props, which don't cast shadows) — the classic single-material-no-postprocessing rim technique. Outline color is each character's own accent, brightened.

**Batch 8 status: complete**, with two items reasoned through a deliberate scope adjustment (see notes above) rather than literally as worded — both times because the literal reading would have meant adding new external dependencies or building bespoke per-character systems, which the brief's own ground rules argue against. Syntax-checked and passed the mock-DOM execution after every change in this batch. Not playtested for visual feel (lean angles, fog distances, tint amounts are reasoned first passes).

---

## Batch 9 — Usability and accessibility

- [x] Remap P2 to a tighter key cluster + full rebinding persisted to `localStorage` — **fixed via full rebinding**, which subsumes the "tighter cluster" ask (P2's default layout — arrows/`/`/K/L/`;`/`.`/`,` — was already a reasonably tight right-hand cluster; a rebind system means any player can retarget it further to their own preference anyway, which is the stronger fix). New `BINDINGS`/`DEFAULT_BINDINGS` (`{p1,p2} × {up,down,left,right,jump,attack,special,skill,dodge,aimlock}`), loaded from `localStorage` (merged over defaults so a stale/partial save from before an action existed doesn't break) and persisted through `safeLSGet`/`safeLSSet` helpers (localStorage can throw, not just return null, in some private-browsing/sandboxed contexts — every read/write in the file now goes through these two). `Fighter.update()`'s P1/P2 input blocks read `BINDINGS.p1`/`BINDINGS.p2` instead of hardcoded key literals. New "Rebind Keys" screen (reachable from the menu and from pause) lists every action with a click-to-capture button; a capture-phase `keydown` listener intercepts the very next key press so it doesn't also fire as a live game action.
- [x] Menu wraps / mobile layout for narrow windows — **fixed.** `.select-row` gained `flex-wrap: wrap` (plus `max-height`/`overflow-y: auto` so an extreme case still scrolls instead of clipping) and `.side-panel` width changed from a fixed `290px` to `min(290px, 90vw)`, so the two player panels + VS column stack into a single column on narrow viewports instead of overflowing.
- [x] Touch controls or explicit "keyboard/gamepad required" message — **explicit message, not touch controls** (the brief offers either). A `pointer: coarse` + `navigator.maxTouchPoints > 0` check shows a warning banner on the menu on touch-primary devices with no separate keyboard signal available, rather than silently failing to respond to taps.
- [x] Handle CDN failure (`onerror` on the Three.js script tag + visible message) — **fixed.** `onerror` on the CDN `<script>` tag plus a `typeof THREE === 'undefined'` guard as the very first line of the game's own script tag (covers a `<script>` tag that "succeeds" with an empty/corrupted response, which `onerror` alone wouldn't catch) both show `#cdn-error`, a full-screen message naming jsDelivr and suggesting an ad/script blocker as a likely cause.
- [x] Fix stale "Top-Down" copy in `<title>`/subtitle now that the camera is angled; unify the game name — **fixed.** `<title>` was `"Echoes of Eternity: Astral Clash - Top-Down"` → now just `"Astral Clash"` (matches the in-game `<h1>`, which was already just "Astral Clash" — the title tag was the one place still carrying the old/unused "Echoes of Eternity" name and the stale camera-angle description). Subtitle `<p>` "Top-Down Dimensional Arena Fighter" → "Dimensional Arena Fighter".
- [x] Prevent/flag duplicate fighter picks; add a "random" option per side — **flagged, not prevented** — deliberately, since Batch 8 built dedicated mirror-match readability (P2 body tint + name tags) on the premise that same-character matchups are a supported scenario, not a mistake to block. `refreshMenuUI()` appends "(Mirror match!)" to the status line once both sides have previewed/confirmed the same character. New "Random" button (and R / `/` keyboard shortcuts) per side, routed through the same `previewPick()` path a manual click uses.
- [x] Accessibility floor: `:focus-visible`, keyboard nav through fighter grid, scalable HUD text, `prefers-reduced-motion` for camera lerp/shake, colorblind-safe P1/P2 palette check — **fixed**, itemized: `:focus-visible` outline added for all buttons. Keyboard nav through the fighter grid relies on the grid already being real `<button>` elements (native Tab order + Enter/Space activation) — the focus-visible outline is what was actually missing to make that usable, not the navigation itself. Scalable HUD text: new `HUD_TEXT_SCALE` toggle (Normal/Large, `localStorage`-persisted) applied via a `hudFont(px, bold)` helper to the load-bearing readouts (per-player HP/meter/skill panel, round timer) — the HUD is canvas-drawn, not real DOM text, so OS/browser zoom doesn't reach it the way it would a normal page, hence an explicit in-game toggle. `prefers-reduced-motion`: new `REDUCED_MOTION` const cuts hit-shake to 15% (not to zero — it's also the main "you got hit" cue for anyone not staring at the HP bar) and flattens the continuous camera zoom/pan that chases both fighters (zoom locked to 1, azimuth/pitch pan cut to 30%) — that ongoing sway is its own motion-sickness trigger independent of getting hit. Colorblind-safe P1/P2 palette: self-checked, not run through a simulator — the two colors actually used to distinguish P1 from P2 (the Batch 8 name-tag sprites, cyan `#00f3ff` vs pink `#f472b6`) differ in both hue and lightness, not a red/green-only distinction, so they should hold up under the common deuteranopia/protanopia forms; not verified against an actual simulator.
- [x] In-fight controls reminder on pause (freeing the permanent HUD strip) — **fixed.** The full control list that used to sit at the bottom of the HUD every frame for the entire match is gone from there; it now only appears on the pause screen (already built in Batch 5, now sourced from the same `controlsSummary()` string the old permanent strip used, so a rebind shows up correctly in both places). The freed strip shows a one-line "Esc for controls / pause" hint plus a compact bot-controlled note instead.
- [ ] Training mode (infinite HP, frame data, hitbox view, reset-to-position) — was implemented (menu toggle, infinite HP via `takeDamage`/`takeHazardDamage` guards, forced debug overlay, `R`-key position reset), then **removed at the user's explicit request**. All of it — the menu button, `trainingMode` flag, the `takeDamage`/`takeHazardDamage` HP guards, the `R`-key reset handler, the forced-on debug overlay, and the no-timeout round-timer guard — has been deleted from `index.html`. Frame data and hitbox view remain available generally via `?debug=1`, just not auto-enabled without it.

**Batch 9 status: complete**, with two items scoped through a deliberate reading of the brief's own "or"/either-or phrasing (touch: message instead of full touch controls; duplicates: flag instead of prevent, matching Batch 8's mirror-match work) rather than the more expensive literal option. Syntax-checked and passed the mock-DOM execution after every change; this batch also surfaced and fixed a real gap in the harness itself — it doesn't define `localStorage`, so the first `localStorage.getItem(...)` call outside a try/catch (`HUD_TEXT_SCALE`'s initializer) threw. Rather than special-case the harness, added `safeLSGet`/`safeLSSet` wrappers and routed every localStorage read/write in the file through them — a real defensive fix (some private-browsing/sandboxed contexts throw on localStorage access too, not just return null), not just a test workaround.

---

## Batch 10 — Audio

- [x] Any audio at all — **fixed.** Full WebAudio synth module (no sample files, per the brief's own ground rules) — `synthTone()` (oscillator + linear-attack/exponential-decay envelope, optional pitch sweep) and `synthNoise()` (decaying filtered white noise buffer) as the two low-level primitives everything else below is built from.
- [x] Per-attack-type impact sounds (slash/punch/hammer/dagger/shard/orb), pitched by damage — **fixed.** `SFX_TYPE_BY_CHAR` maps each character to one of the six identities already used by `createEffectMesh`'s visual effects (Kaelen→slash, Lyra→shard, Gorgonok→hammer, Voss→dagger, Draven→hammer, Seraphine→orb), so sound and visual match. `sfxImpact(charName, damage)` pitches down (not up — reads as "weightier") and raises volume with `damage`, called from `takeDamage()`'s attacker branch.
- [x] Wind-up cue during startup frames — **fixed.** `sfxWindup(charName, kind)` fires from `tryStartAction()`, the instant `atkState` enters `startup` — a short rising tone, pitch varying by `kind` (basic/special/skill), audible even if you're not watching the Batch 8 lean animation.
- [x] Whiff sounds distinct from hits — **fixed.** New `this.hitLanded` flag, reset `false` in `tryStartAction()`, set `true` by `takeDamage()` when this fighter is the attacker. `updateActionState()`'s active→recovery transition checks it: if nothing connected during the active window, `sfxWhiff()` (a short bandpassed noise swoosh, textured differently from any impact sound) plays instead of silence.
- [x] Special activation stings per character — **fixed.** `sfxSpecial(f)` derives a root frequency from `hexNum(f.color) % 300` — cheap way to get six different-sounding stings (one per character's own accent color) without six hand-authored cues. Fires from `updateActionState()` alongside `doSpecial()`.
- [x] Hazard warning rise → activation sound, pickup collection chime — **fixed.** `sfxHazardWarn()` (rising sawtooth) on the `dormant`→`warn` transition, `sfxHazardActive()` (low noise thump + square-wave hit) on `warn`→`active`, both in `updateHazard()`. `sfxPickup()` (two-note ascending chime) on pickup collection in `updatePickup()`.
- [x] Low-HP heartbeat, round start/end cues, KO impact — **fixed.** `sfxHeartbeatTick(arenaTime)` — self-throttled to roughly once every 45 frames via a shared `lastHeartbeatAt`, so both fighters being low simultaneously doesn't double the rate — called from the existing low-HP-particle block (`this.hp/this.maxHp < 0.25`). `sfxRoundStart()` in `startRound()`, `sfxRoundEnd()` in `endRound()`, `sfxKO()` at the exact moment `gameLoop()` detects `hp <= 0` and transitions to `DEATH`.
- [x] Ambient arena pad per map — **fixed.** `startAmbientPad(map)`/`stopAmbientPad()`: two detuned sine oscillators (a root note nudged per-map by `hexNum(map.accent)`, plus a slightly-sharp fifth that beats slowly against it) through a slow-LFO'd lowpass filter, so each arena has its own faint drone rather than dead silence between action sounds. Called from `loadMap()` (which already runs once per round via `startRound()`), and explicitly stopped when returning to the menu (`btn-play-again`, `btn-quit-to-menu`) so it doesn't keep playing over the select screen.
- [x] `AudioContext` initialized on first user gesture; master/SFX/music volume + mute, persisted to `localStorage` — **fixed.** `initAudio()` is never called eagerly — `unlockAudio()` (bound to the first `keydown`/`pointerdown` via `{once: true}`, and also called explicitly when opening the new Audio Settings screen) constructs the `AudioContext` and resumes it if suspended, which is what autoplay policies actually require. Three gain stages (`masterGain → sfxGain`/`musicGain → destination`) driven by a new Audio Settings screen (master/SFX/music sliders + a mute toggle), `localStorage`-persisted via the same `safeLSGet`/`safeLSSet` helpers Batch 9's bindings/HUD-scale settings use.

**Batch 10 status: complete.** Syntax-checked and passed the mock-DOM execution — every `sfx*`/`synth*` function guards on `if (!actx) return`, so with no real user gesture ever firing in the harness (and thus `actx` staying `null` for the whole run), every audio call site exercised during the harness's top-to-bottom script execution was a confirmed no-op rather than a crash. Not aurally verified — I can't play sound to check tuning/balance/mix, so the actual pitches/durations/volumes above are a reasoned first pass (matching the visual-effect identities and character accent colors already in the code), not a mixed/mastered result.

---

## `?debug=1` overlay (introduced in Batch 0, needed by every later batch)

- [x] **Added, then extended twice more.** `const DEBUG = new URLSearchParams(location.search).get('debug') === '1'`. `drawDebugOverlay()` (called from `drawHUD()`) draws: hurtbox circle, facing vector, the active melee hitbox cone (only during the `active` frame window), in-flight projectile hurtboxes, FPS/dt/hitstop readout, and each fighter's full state line (`atkState` + timer, grounded/air, z, hitstun, dodge cooldown, bot flag). Batch 7 added a draw-call/triangle counter (`renderer.info.render.calls`/`.triangles`) to the same readout line. Batch 9's training mode forces this overlay on (`if (!(DEBUG || trainingMode) || ...)`) without needing the URL flag, since hitbox/frame-data visibility is half the point of training mode.

---

## Final status: all 11 batches (0 + 1-10) complete

Every batch above is marked complete, working through the standing instruction to continue through Batches 3-10 as one continuous pass rather than stopping after each. Two recurring patterns across the back half worth calling out here rather than repeating per-batch:

- **Verification.** Every single edit in this file, across all 11 batches, went through the same two-step check: `node --check` (syntax) followed by executing the whole script under the mock-DOM/THREE.js harness (`mockharness.js`, described at the top of this file) to catch runtime `ReferenceError`s syntax-checking can't see. That combination is what caught both TDZ regressions documented in this file (the original `camera` one, and Batch 7's `geometryCache`/`disposeObject3D` one) — neither would have been caught by `node --check` alone. The final full-file run of both checks (after Batch 10) passed clean.
- **Scope calls.** A handful of items (Batch 7's mesh "pooling", Batch 8's rim/outline pass and off-camera hit indicator, Batch 9's touch controls and duplicate-pick prevention) were implemented through a more conservative reading than the most literal one, each time because the literal reading would have meant a new external dependency, a bespoke per-character system, or contradicted a feature built earlier in the same pass (mirror-match support vs. "prevent duplicates"). Each is called out inline in its batch section above with the reasoning — flagging here since it's the one place scope was interpreted rather than executed as written.

What this checklist **can't** confirm, because it requires an actual browser and a human playing: real play-feel/balance (frame-data numbers, HP/damage tuning, lean angles, audio mix/tuning), the "Verification checklist" items below, and anything about how it actually *feels* to play a full match. Everything else — every described bug fix, system, and file-level change — has been implemented and is traceable to a specific diff in this file's history.

---

## Post-brief review pass — Part A/B/C bugs + Changes 1-3 + UI refinement (2026-08)

A later review prompt covered 21 correctness bugs (A1-A21), 15 UI items (B1-B15), 3 refactors (C1 screen manager / C2 keyboard dispatcher / C3 reduced-motion), then three follow-up "Changes" (1: vendor three.js as offline fallback + wrap game in `bootGame()`; 2a: move state-changing feedback FX out of `render3D` into `update()`/`updateFeedbackFx`; 2b/3: optional table-driven loop + FighterView split). Each shipped as its own revertable commit — see `git log` for the per-item messages, which carry the root-cause detail that used to live in the batch sections above. Verification for this pass added `realthree_check.js` (runs the page against the *real* downloaded r128 build, drives every character's basic+special animation, loads all 10 maps, builds thumbnails) on top of the existing `node --check` + `mockharness.js`.

**C2 — single keyboard dispatcher.** Keyboard handling was five separate `keydown` listeners whose correctness depended on capture-vs-bubble phase ordering and each one early-returning to stay out of the others' way: a global key-state tracker (bubble), a rebind-capture listener (registered on the *capture* phase specifically so it could `stopImmediatePropagation` before the others), a first-gesture audio unlock (`{once:true}`), the menu fighter-select handler (bubble), and the Escape→close-modal-or-pause handler (bubble). That's fragile — the phase trick was load-bearing and invisible. Merged into one bubble-phase handler with explicit top-to-bottom precedence: (1) rebind capture wins and consumes the key (still kept out of `keys[]`/`pressed` so a captured key doesn't read as held when the screen closes), (2) first keypress unlocks audio via a `kbAudioUnlocked` gate, (3) global key-state update, (4) Escape closes the top modal or toggles pause, (5) fighter-select nav (menu + no-modal only). The capture-phase listener and the keydown audio `{once:true}` listener are gone (pointer keeps its own one-shot). Verified with `c2_check.js`, which captures the single registered handler and fires synthetic events: rebind capture sets the binding without touching `keys[]`, a normal key sets `keys[]`, Escape closes an open modal without pausing (A5) then pauses/resumes when none is open, and a roster key previews a fighter — all pass, one listener registered.

**Change 2b — table-driven gameLoop.** The frame loop's middle was a four-arm `if/else` ladder on `gameState` (FIGHT / INTRO / DEATH / ROUND_END). Extracted each arm verbatim into a `stepFight/stepIntro/stepDeath/stepRoundEnd(dt)` function and dispatch through a `STATE_STEPS` table (`STATE_STEPS[gameState]?.(dt)`). `gameLoop` now holds only the frame-wide concerns: dt/fps, the PAUSED and hit-stop freeze short-circuits, `updateHitFeedback`, the table dispatch, then the shared `decayCameraShake`/`syncViewmodels`/`renderViews`/rAF tail. States with no per-frame work (MENU) simply have no table entry, matching the old ladder's "fall through to the shared tail". Purely structural — no behavior change. Verified with `stepdrive_check.js`: each of the four steps runs once via the table against real three.js without throwing, and `STATE_STEPS['MENU']` is `undefined` as intended.

**UI refinement sub-batch (earlier commit):**
- **B4 button tiers.** Base `button` is now `width:auto` (was full-width via `!important`); `.menu-section button` restores stacked full-width where wanted. Added `.btn-secondary` (cyan outline) and `.btn-tertiary` (slate ghost). `.btn-back` and the map-random button restyled onto those tiers instead of ad-hoc rules.
- **B5 control affordances.** Bot-mode control is an on/off switch (track + sliding knob via `::before`/`::after`, amber when `.active`). The Random-pick buttons no longer borrow `.bot-toggle` (they're one-shot actions, not toggles) — now `.btn-secondary`. Bot difficulty is a cycler: `.btn-cycler` styled with `‹ Normal ›` guillemets so it reads as a stepper (text set in both the HTML and the JS setter).
- **B12 de-emoji.** Dropped decorative emoji from the settings buttons (🕹/🔠/🔊), the Settings button gear (⚙), Random Arena (🎲), and the gamepad tutorial line (🎮). Where an icon carried meaning it became an inline SVG: bot-mode die and the pad badge (controller). Verified zero non-ASCII symbols remain in the top-of-file HTML.
- **B13 selected state in the roster.** Picking a fighter used to hide the whole grid and swap in the detail panel, so you lost sight of the roster. Now `showDetail` keeps the grid rendered (`display:grid`), marks the chosen card `.selected` (lit accent, dark label, key pill kept readable), and shows the detail below it. On confirm the grid gets `.locked` (dimmed + `pointer-events:none`, with the selected card kept full-brightness), so the pick reads as final without vanishing. `showGrid`/`backToPick` clear the lock and the highlight. Smoke-tested through preview→confirm→back→random under the DOM stubs (`b13_check.js`): grid stays visible the whole time, locks only between confirm and back.
- **Live-feedback fixes.** Removed the split-screen HUD divider line (read as a color seam). `#ui-overlay { padding: 16px 0 44px }` so the Settings button isn't cut off at the bottom when scrolled. Rebind key buttons made uniform-width (110px), slate, left-aligned in flex rows with 64px action labels (they'd regressed to full-width cyan blocks under B4's `.menu-section button` rule). Removed all `.tutorial-card-icon` emoji and the now-redundant "Above All Else, have fun" card (grid no longer needs a filler cell for a multiple-of-three count).

---

## Mobile pass (2026-08-10)

Resumed after a cut-off session. Two things, both actually visually verified this time — a real headless Chrome (via `puppeteer-core` driving the system-installed Chrome, no download needed) was available, closing the "not visually verified" gap that every prior batch above had to leave open.

**Bug found and fixed: map-select thumbnails were invisible.** The `buildMapThumbnail()` canvas art described in the "Map-select screen" section above was correctly drawing a full top-down plan (floor, columns, terrain, spawns) — confirmed by reading pixel data straight off the canvas — but a global `canvas { position: absolute; width:100%; height:100% }` rule (written for `#gameCanvas`/`#hudCanvas` only, but matching every `<canvas>` on the page since it had no selector scoping) pulled each thumbnail out of layout flow inside its `.map-card`, collapsing the card to ~4px tall. The art was there; it was just rendered into a sliver too thin to see, which read as "just a color" (the only thing left visible at that height was the 3px accent-bar). Scoped the rule to `#gameCanvas, #hudCanvas` by ID. Verified with a headless-Chrome screenshot of the actual map-select grid: all ten themed thumbnails now render at full size and are clearly distinguishable.

**Mobile version added, on request.** This game had no touch input at all before this pass — a `pointer: coarse` check just showed a "keyboard/gamepad required" banner (Batch 9's deliberate scope call, made when full touch controls were a bigger feature than that pass covered). Added:
- *Touch controls:* a virtual joystick (turn + forward/back, mirroring the keyboard's turn-then-walk FPS scheme) and a 4-button cluster (Jump/Special/Dash/Attack) per human side. Both write directly into the same `keys{}`/`pressed` state the keyboard dispatcher already populates (`touchKeySet()` mirrors the real keydown/keyup handlers' edge-detect logic) — `update()`, `stepDeath()`'s death-skip check, bot logic, none of it needed to change; touch is just another writer of input state the rest of the game already trusts. Keyed off the *live* `BINDINGS.p1`/`BINDINGS.p2` so a rebind still resolves correctly even though the touch pads themselves are fixed-function. Shown only for sides that are actually human (a bot side gets no pad) and only on touch-primary devices (`pointer: coarse` or `hover: none`, gated on `maxTouchPoints > 0`) — never on a mouse, including touch-capable laptops with a fine primary pointer. When both sides are human, the pads mirror left/right (joysticks at the outer edges, buttons toward the middle) the same way the split-screen view already divides.
- *Touch pause button:* no physical Escape key on a touchscreen, so a small "II" button appears top-center during play, calling the same `togglePause()` the keyboard's Escape branch already used (which safely no-ops outside FIGHT/DEATH/ROUND_END, so it can't mis-fire mid-intro).
- *Landscape prompt:* the FPS view and its HUD are laid out for a wide arena; in portrait the whole scene reads as a squeezed sliver, and reworking the HUD's coordinate scaling to support arbitrary aspect ratios well was judged a much larger, riskier change than this pass called for. Rather than ship a distorted or silently-broken portrait view, `#orientation-prompt` blocks play with a rotate-device message whenever a touch device is in portrait during FIGHT/INTRO/DEATH/ROUND_END — the menu/select/map-select screens stay fully usable in either orientation (they already were, from Batch 9's responsive work).
- *Full-bleed on touch devices:* `#game-container` drops its border/shadow/rounded corners and fills the full viewport under `(pointer: coarse), (hover: none)` instead of the desktop `min(96vw, 1170px)` boxed look, with `env(safe-area-inset-*)` padding for notches.
- Verified end-to-end with headless Chrome under mobile emulation (touch + mobile UA, both landscape and portrait viewports): touch pads render and are correctly split/mirrored for two humans and repositioned full-width for a solo human vs. bot; dragging the joystick via a simulated pointer actually walks the fighter forward (confirmed by the camera advancing through the map between before/after screenshots); tapping Attack shows the press state and the special-meter tick that follows a real `tryStartAction` call; the pause button opens and the Resume button closes `#pause-screen`; the portrait prompt appears during a fight and stays absent from the menu. Zero console/page errors across all of the above. Not verified: a real physical touchscreen (button/joystick sizing, hit-target comfort, and true multi-touch — two fingers down at once on a split-screen two-human match — are a reasoned first pass from a simulated single-pointer driver, not a hands-on result).

---

## Progression, co-op, and new-modes pass (2026-08-11)

A large multi-batch feature arc, on request: meta-progression (coins/unlocks/upgrades), a reversible debug "unlock everything" toggle, co-op boss/survival modes, three new objective-based modes (Zone Control, Time Attack, Survival Waves), a platform-reachability fix, and camera pitch. Planned as ten batches (numbered 11-20, continuing this file's own numbering) before any code was written, specifically because the render/camera/HUD/win-condition code turned out to be hardcoded to *exactly two* combatants in more places than just the obvious ones — see each batch's own notes for what that actually required.

**Batch 11 — Reachability audit + fix.** Computed real jump-chain reachability from the live physics constants (`JUMP_VZ=9, GRAVITY=0.42` → max single-jump height gain = `VZ²/(2·GRAVITY) ≈ 96.4` units) against every map's actual platform data, rather than trusting this file's own prior prose about which platforms are "hard to reach" (a standalone audit script, `reachabilitycheck.js`, confirmed several of this file's own past claims about specific heights were already stale from later re-tuning passes — expected, not a new problem, per the standing "verify against live data, not old comments" lesson this file has run into before). **Found a genuine, previously-unnoticed bug:** Sundered Stair's climbing chain had a platform-to-platform gap of 100 units (110→210) — mathematically impossible to jump, exceeding the 96.4-unit max with no margin at all. This wasn't the "climb tier by tier, deliberately hard" design the surrounding comment describes elsewhere in this same map (a genuinely tight-but-possible 90-unit gap exists safely on Skyreach Spire) — it was simply too tall. Retuned the platform from height 210 to 200 (now a 90-unit gap, matching Skyreach Spire's own tightest safe margin). Re-ran the audit against all ten maps afterward: every platform below the file's own documented ~250-height "these are unjumpable interior walls, not perches" line now has a climbable gap; the handful of platforms at or above that line (Twin Ramparts, The Colosseum Ring, Skyreach Spire, The Shattered Bridge, Molten Foundry, Overgrown Sanctuary) were confirmed as exactly that — intentional lane-divider walls with 175-215-unit gaps, correctly left untouched rather than "fixed" into something they were never meant to be. Verified via a full headless-Chrome load/regression pass (zero console errors) on top of the standalone math audit.

**Batch 12 — Progression foundation (coins, unlocks, upgrades, Shop).** The game previously handed you all ten fighters immediately with nothing to earn. Added a full meta-progression layer:
- *Save shape:* `astralClashProgression` → `{coins, unlockedChars, upgrades: {CharName: {hp,dmg,special,speed}}, doubleJumpUnlocked}`, loaded with the same merge-over-defaults idiom as `BINDINGS` (plus explicit repair of `unlockedChars`/`upgrades`/`coins`, the three fields anything downstream indexes into directly, so a hand-edited or half-written save degrades to defaults instead of throwing).
- *Single source of truth:* `isCharUnlocked(name)`, `upgradeLevel(name, trait)`, `isDoubleJumpUnlocked()`, `upgradeMult(name, trait)`, `unlockedCharNames()`. Every gameplay and UI check goes through these rather than reading `progression` directly — deliberately, so Batch 13's debug override has exactly one place to hook rather than N scattered checks to keep in sync.
- *Numbers:* three starters (Kaelen/Lyra/Draven — melee/ranged/tank, so a new player can feel out all three archetypes); unlock costs 300-750 tiered loosely by kit complexity; 4 upgrade traits × 3 levels at 150/300/500 coins, **+5%/level (+15% maxed)**, deliberately modest so a fully-upgraded starter never eclipses a stock top-tier pick; Double Jump 1000 (account-wide capstone, priced above any single unlock); coins 50/win, 30/draw (loss payouts wired in with the per-mode modes in later batches, so a losing streak still makes progress rather than hard-sticking).
- *Applied to real gameplay,* not just the menus: the `Fighter` constructor scales `maxHp`/`speed`/`attackDmg`/`meterRateMult` by `upgradeMult(...)`, which returns exactly 1.0 on a fresh save — so the pre-progression numbers are reproduced bit-for-bit until something is actually bought. Guarded off for a boss/wave entity (`isBossEntity`) so Batch 16+'s difficulty/wave scaling can't double-dip with player upgrades.
- *UI:* locked roster cards get a new `.roster-locked` class (desaturated, price pill swapped in where the hotkey pill sits, click routes to the Shop instead of a dead button) — **named distinctly from the pre-existing `.locked` grid class on purpose**, since that one means "a pick is confirmed, dim the whole grid" and the two concepts must not collide. Because `buildGrid` only runs once at load but lock state changes at runtime (a purchase, or Batch 13's override), the visuals are applied by a separate `refreshGridLocks()` pass over the already-built cards rather than by rebuilding the grid. New `#shop-screen` modal (following the `mapselect` precedent) with per-character upgrade pip rows + unlock buttons; the fighter detail panel now shows **effective** (upgraded) stats with a `+N` level badge, so a purchase visibly changes something immediately instead of only mattering mid-match. Bot sides and the Random button both roll from the unlocked pool only.
- *Two real CSS bugs found and fixed while building this,* both the same class of specificity/box-model trap this file has hit before: (1) `.shop-panel` is itself a `.menu-section`, so `.menu-section button { width: 100% }` (class+element beats a lone class) stretched every shop button full-width and crushed the labels beside them — fixed by qualifying the rule as `.menu-section .shop-buy`, the same fix the bot-toggle needed. (2) Every modal panel sized itself with `max-height: 90vh`/`92vh`, but modals are positioned against `#game-container`, not the viewport — on any window taller than the container (the normal desktop case) `90vh` resolves *larger* than the parent, so long panels overflowed their centered parent and were clipped at **both** ends with nothing scrollable to reach them. That's why the tutorial and shop titles were cut off. Fixed for all six modal panels at once with `max-height: 100%` + `box-sizing: border-box`.
- *New verification tooling:* `?debug=1` now also exposes a `window.ACDebug` handle on the game's internals (state getters + the progression accessors/mutators + `Fighter`). Everything in this file lives inside `bootGame()`'s closure — good hygiene, but it left automated checks with nothing to assert on but pixels, which is why several past batches could only be "verified" by screenshot. Gated behind the same flag as the on-screen debug overlay, so a normal player's page still exposes nothing. `progressioncheck.js` uses it to assert 31 behaviors: fresh-save state, purchase affordability/deduction/re-buy rejection, per-trait independence, level caps, the multiplier landing on a real `Fighter` instance (and *not* leaking across traits), persistence across a reload, live grid-lock updates, and that the bot/random roll pool never contains a locked character. All pass, zero console errors, plus the existing desktop/touch/reachability suites still green.

**Batch 13 — "Unlock everything" sandbox override (reversible).** Two entry points, one setter (`setDebugUnlockAll`): a **typed cheat code** (`astral`) and a **Settings checkbox**, per the request for both. The load-bearing design decision is that this is an *override layer*, never a mutation: `debugUnlockAll` lives under its own localStorage key (`astralClashDebugUnlockAll`), and the Batch 12 accessors (`isCharUnlocked`/`upgradeLevel`/`isDoubleJumpUnlocked`) simply return the unlocked answer when it's set. Nothing ever writes into `progression` while it's on, which is what makes "turn it off and get your exact save back" true by construction rather than by a save/restore dance that could go wrong. Scope is broad (all characters + all upgrades maxed + Double Jump), matching the intent of a sandbox to see everything the game has.
- *Cheat-code buffer placement mattered.* The keyboard handling is a single merged dispatcher with explicit ordered precedence (the C2 pass above). The buffer is inserted as a new step **after** the rebind-capture early-return (so a key being captured for a rebind can never reach the buffer — verified) and **after** the `keys[]`/`pressed` update (so it never consumes or blocks a real input; typing part of the code still moves your fighter). A 6-character code was chosen so its exact ordered letters can't plausibly occur during play.
- *Confirmation is a DOM toast, not a HUD-canvas one*, because `drawHUD()` only runs during a fight and the code is most often typed on the menu — a confirmation you can't see is worse than none. Placed bottom-right after the first attempt (centered-top covered the game title; centered-bottom collides with the in-fight hint line and the mobile touch buttons), `pointer-events: none` so it can never eat a click.

**Batch 14 — Bot mode gated to the real progression state.** Bot matches are available in normal play (so solo players can grind unlocks) and disabled while the sandbox override is on — a maxed-out, everything-unlocked state farming coins against a bot would make the progression it's bypassing pointless. Implemented in `refreshBotUI` (force the side's `isBot` false, `disabled` the toggle, and set a `title` explaining *why* it's greyed rather than leaving it silently inert) plus a guard in each toggle's click handler. Verified that clicking the disabled toggle cannot set `p1IsBot`/`p2IsBot`, and that turning the override off re-enables it.

Verified for both batches with `cheatcheck.js` (33 assertions, all passing): the code typed as **real keydown events through the actual dispatcher** flips the override; every character/upgrade/Double Jump reads unlocked; the real save's coins, `unlockedChars`, `upgrades`, and `doubleJumpUnlocked` are **byte-identical** while it's on and restore exactly when it's off (including a locked character re-locking and an upgrade level returning to its real value); a normally-locked fighter is genuinely pickable in the sandbox; both bot toggles disable and re-enable; the override itself persists across a reload with the real save still intact underneath; and — the case most likely to break — arming a rebind and then typing the code consumes the first key as the new binding and does **not** toggle the override. Existing progression/desktop/touch/pause suites all still green, zero console errors.

**Batch 15 — Double Jump upgrade + every high platform made reachable.** The purchasable (1000-coin, account-wide) air jump, plus the map retuning it exists to justify.
- *Mechanic:* `Fighter.usedDoubleJump`, one air jump per airborne stretch, re-armed on any landing (ground, step, or platform). **Reset-style** (`vz` is set back to `JUMP_VZ`, not added to the current `vz`) — the standard, better-behaved form, and it gives a bounded ceiling instead of an additive one that could be stacked at liftoff to trivialize everything. A small ring puff fires on use so the air jump reads as deliberate rather than as a physics glitch.
- **The non-obvious bug this had to avoid.** The ground jump reads a *held* key (`keys[b.jump]`), which is fine because `grounded` is what stops it re-triggering. A mid-air second jump can't work that way: the same physical press that started jump 1 is almost always still down on the very next frame, so a held-state check would auto-spend the air jump one frame after takeoff with no second press from the player at all. Fixed by tracking a genuine key-**down edge** for the air jump only (`consumePress(b.jump)`; nothing else consumes the jump key's press, and the gamepad's `gp.jump` was already edge-triggered), leaving the ground jump's held-state check untouched so existing jump feel is bit-for-bit unchanged. This is verified directly: holding the key peaks at a single jump's height (~92 gain), not a double's (~167).
- *Scope cut:* human input only. Bots don't need it (their pathing never routes through these perches) and giving them a movement option the AI wasn't written around risks worse behavior, not better — asserted in the checker that a bot never sets `usedDoubleJump`.
- **Every "unreachable" high platform is now genuinely reachable, which was the actual request.** Measured the best-case air jump against a real `Fighter` by sweeping the second-press frame: **~184 units of gain, peaking when the press lands on the first jump's apex frame** (21). Then audited every map's tall blocks against that. Four wall-tops (Twin Ramparts 285, Molten Foundry 270, Overgrown Sanctuary 250, The Colosseum Ring 250) sat at gaps of 188-215 — flatly unreachable even with the upgrade — and a fifth (The Shattered Bridge 270, gap 175) was reachable only with a frame-perfect apex press, which isn't "reachable" in any sense a player would recognize. All five pairs were lowered so every gap is **160-163**, leaving a wide timing window rather than demanding a pixel-perfect input. Each is still 222-255 units tall against a 46-unit eye height, so their actual job — blocking sight lines as interior lane dividers — is completely unchanged; only their tops became attainable. (These are thin 26-30-half-width dividers, so the reward is a narrow high perch, which pairs naturally with the look-up/down aiming in Batch 20.)
- *Checker improvement worth noting:* `reachabilitycheck.js` now classifies a platform as wall-vs-perch by **footprint thinness** (`min(hw,hd) <= 32`) rather than by height. Height gets re-tuned — this batch changed five of them — so a height threshold would have silently reclassified a wall as a perch the moment it was adjusted, and quietly stopped checking the thing it was written to check. It runs two passes: ordinary perches must be reachable *unaided*, and **every** platform including wall-tops must be reachable *with* Double Jump. Both pass for all ten maps.
- Verified with `doublejumpcheck.js` (16 assertions, all passing — including the held-key trap, one-air-jump-only, re-arm-after-landing, no-op before purchase, bots never using it, and the best-case-timing sweep clearing the retuned gaps) plus the reachability audit and the existing progression/cheat/desktop/touch suites, all green, zero console errors. Tutorial's Movement card updated per the standing tutorial-sync rule.

**Batch 20 — Camera pitch (look up/down) + pitched projectile aiming.** Done out of numeric order, immediately after Batch 15, because it's the other half of the same problem: Batch 15 made the tall perches *reachable*, and this makes them *fightable* — before it, someone on a high platform couldn't be shot at all, since projectiles only ever flew dead level.
- *State:* `Fighter.pitch` (radians, positive = up) + `lookBy(radians)`, clamped to ±42° — enough to sight the tallest perch from the floor and to shoot down from one, without letting the view roll past the horizon. Deliberately kept **separate from `fx`/`fy`**, which stay a purely horizontal facing vector, so every existing 2D movement / melee-cone / bot-steering calculation is untouched.
- *Input, following the established binding pattern rather than a bespoke path:* new `lookUp`/`lookDown` entries in `DEFAULT_BINDINGS` (**P1 R/V**, **P2 `;`/`'`** — free keys beside each side's existing cluster), which automatically flow into the rebind screen (now 20 rows) and `controlsSummary()`. Gamepad uses the **right stick's Y axis** (`axes[3]`) — an axis, not a button, since all four face buttons are already spoken for and analog pitch is what you actually want; a pad reporting fewer axes yields 0, i.e. exactly the pre-batch behavior. Touch gets a **drag-to-look zone** in the upper part of each side's pad area, mirroring the existing `touchTurn` analog pattern with a new `touchLook`; it returns to level on release, since a sticky pitch with no visible cause reads as a broken camera.
- *Camera:* `positionFpsCamera`'s look-target height, previously a hardcoded `-4` (or `-22` during the intro drop), is now `-4 + tan(pitch) * LOOK_DIST`. The intro drop keeps its own hardcoded steep tilt — it's a cinematic, not something the player aims through.
- *Projectiles inherit the shooter's aim,* which is the part that actually matters for gameplay: a new `projectileVelocity(speed, fx, fy, pitch)` splits speed into horizontal (`cos`) and vertical (`sin`) components, so a pitched shot is **redirected, not sped up** — total velocity is preserved, keeping range and travel-time feel identical. Straight-line, no gravity, as intended. Both spawn sites (the basic `fireProjectile` and Lyra's volley fan) use it, and the travel loop gained one line (`if (p.vz) p.z += p.vz * dt`). `verticalHit` needed **zero** changes — it was already written against an arbitrary `p.z`.
- *Also needed, and easy to have missed:* vertical despawn bounds. A downward shot would previously have sunk through the floor forever and an upward one climbed forever, both leaking meshes, because the existing despawn only tested horizontal arena bounds — a shot going straight up never crosses them. Downward shots now bury into the surface under them (with a small dust ring so it reads as an impact rather than a vanish) and upward shots die at a `PROJECTILE_CEILING`.
- **Regression safety, asserted rather than assumed:** at pitch 0, `projectileVelocity` returns exactly `(fx*speed, fy*speed, 0)` and the travel loop's `if (p.vz)` guard skips entirely — so every bot-fired shot (bots never set `pitch`) is bit-for-bit what it was before this batch, and the bot's existing projectile-lead math stays correct with no changes.
- **A real regression this batch introduced and the tests caught.** The new touch look-zone initially spanned the top 55% of each side with `pointer-events: auto`, and as a later DOM sibling it painted over `#touch-pause-btn` and swallowed its taps — the mobile pause button silently stopped working. Caught because the pause suite started reporting *inverted* results (tap didn't pause; the subsequent Resume click paused instead), consistently across three runs rather than flakily, which is what distinguished a real bug from the timing race that had produced a similar-looking failure earlier. Fixed by starting the zone below the button's row and giving the button a higher `z-index`. Also hardened the pause checker to `waitForFunction(gameState === 'FIGHT')` instead of a fixed sleep, since `togglePause()` deliberately no-ops outside FIGHT/DEATH/ROUND_END and tapping too early looked identical to a real failure.
- Verified with `pitchcheck.js` (27 assertions) and `touchlookcheck.js` (7). The headline pair: a **level** shot provably *cannot* hit a target 150 units up (the exact limitation this batch removes), while a shot **aimed** at it — pitch computed from the real geometry, `atan(Δheight / distance)` ≈ 20° — connects; likewise a 37°-downward shot from a height-200 perch hits someone on the floor. Plus clamping, proportional (non-snapped) look, speed preservation, camera rotation responding in both directions, despawn bounds, binding/rebind/controls-summary wiring, live keyboard drive through the real `update()` loop, and the bot-unchanged regression. All prior suites (progression, cheat/sandbox, double jump, reachability, desktop, touch, pause) still green, zero console errors. Tutorial's First-Person View card updated per the standing tutorial-sync rule.

**Batch 21 — Progression split per player; bot mode moved into the sandbox (both on request).** Two corrections to Batches 12-14, which had built a single shared account and had the bot rule backwards.
- *Per-player progression.* `astralClashProgression` is now `{p1: {...}, p2: {...}}`, and **every** accessor/mutator takes a side as its first argument (`isCharUnlocked(side, name)`, `upgradeLevel(side, name, trait)`, `buyUnlock(side, name)`, `addCoins(side, n)`, …) — there is deliberately no longer any such thing as "the" progression, so a call site that forgets whose account it means fails loudly rather than silently spending the wrong player's coins. `Fighter` reads its own side's upgrades (via `isPlayerOne`), which means a **mirror match now correctly gives the two Kaelens different stats** if their owners have bought different upgrades. Each side panel shows its own purse plus a shortcut into its own Shop tab; the Shop itself gained Player 1 / Player 2 tabs rather than being duplicated. Grids lock independently, so the two rosters can legitimately differ.
- *Migration, not a reset.* A Batch-12-era save (single blob with `coins` at the top level) is detected and copied to **both** sides. Splitting the format shouldn't cost anyone progress they already earned, and picking one player to inherit it would have been arbitrary.
- *Coins now depend on who wins* — `endMatch` pays `classicWin` to the winner and `classicLoss` to the loser (both sides get `classicDraw` on a draw), each into their own purse, and the game-over summary shows both figures. A loss still pays something so the losing side keeps inching toward their next unlock rather than being hard-stuck.
- *Bot mode is sandbox-only now (inverted from Batch 14).* Progression matches are strictly two humans, so unlocks can only be earned against a real opponent; the "unlock everything" sandbox is where you go to fight a bot or try the whole roster. Correspondingly, `addCoins` is a **hard no-op while the sandbox is on** — since the override deliberately never writes to the real save, letting it *pay into* that save would have turned the sandbox into a farming shortcut for the very progression it bypasses. Turning the sandbox off also clears any bot side that was enabled inside it, so a bot match can't leak into progression play.
- **A TDZ bug caught immediately by the checkers** — the fourth time this file has hit this exact class. `shopSide` was declared with `let` next to `buildShop()` far down the file, but `refreshCoinDisplays()` reads it and runs during initial load, so the whole script died on `Cannot access 'shopSide' before initialization` and `window.ACDebug` never appeared. Moved the declaration up with the rest of the progression state, with a comment naming the trap (same root cause as the historical `camera` and `geometryCache` regressions).
- *Test fallout worth recording,* because it's the intended product change biting the harness rather than a bug: four scripts drove a **bot** match to get a full-screen (non-split) view, which silently stopped working once bot mode became sandbox-only — `sideReady('p2')` never became true, so Start Match never appeared and the scripts crashed. Each now enables the sandbox first, which is genuinely what a human wanting a bot match has to do too.
- Verified with `progressioncheck.js`, now **45 assertions**, including a dedicated per-player-isolation block: P1's coins/unlocks/upgrades/Double Jump provably do not leak to P2, P2 cannot spend P1's coins, each grid shows its own lock count, the mirror-match stat divergence holds, the winner is paid more than the loser into the correct purse, the sandbox banks nothing and leaves both purses intact, and a legacy shared save migrates to both sides. `cheatcheck.js` updated for the inverted rule (bot toggle enabled *in* the sandbox, disabled outside it, un-clickable outside it, and cleared on sandbox-off). All other suites (double jump, pitch, touch-look, reachability, desktop, pause) still green, zero console errors.

**Batch 19 — Game-mode select + Zone Control + Time Attack.** Done before the co-op batches because these two are still pure P1-vs-P2 (no third combatant), so they're far lower risk — and they build the mode plumbing the co-op modes will plug into.
- *`matchMode` is deliberately orthogonal to `gameState`.* `gameState` says where we are in the round lifecycle; `matchMode` says what winning means. It defaults to `'classic'`, and every mode-specific branch is an explicit check against it, so Classic's behavior is unchanged byte-for-byte and a mode that doesn't care about a system never appears in it. A new `#modeselect-screen` modal follows the `mapselect` precedent (opened mid-flow, closes back to what was underneath) rather than adding a top-level screen, reached from a button under the branding — **not** a gate in front of fighter-select, since `'classic'` is already a valid choice; same flag-don't-block stance as the existing mirror-match note.
- *Both new modes are single continuous matches with respawns,* deliberately skipping the best-of-3/pip machinery instead of trying to nest "best of 3 first-to-3-KOs" — that composition was never asked for and would have meant resetting score mid-match and syncing it against collapse timers for no benefit. New `Fighter.respawn(spawn)` is the shared primitive: full HP at spawn, brief i-frames so you can't be spawn-camped, and an **explicit clear of everything that could otherwise carry across a death** (hitstun, root, burn DoT, shield, in-flight action state, queued follow-ups, live projectiles) — leaving any of those attached to a "fresh" fighter is exactly what produces someone who respawns already staggered or on fire. The KO animation still plays in full; only what follows it changes.
- *Zone Control:* a ground ring at arena centre; standing in it **alone** banks control time, contested or empty banks nothing — the simplest rule that keeps "knock them out of the ring" as the core play. Containment is a flat 2D test that **ignores height, on purpose**: several maps have a platform essentially at arena centre, so a floor-only test would have made the zone unreachable there; ignoring z means standing on that platform also contests the ring, which is free verticality rather than a bug. The ring tints and pulses to whoever holds it, so control is readable from inside the fight and not only on the HUD. Arena Collapse converges on the same centre point, so the closing walls squeeze both fighters toward the contested spot instead of fighting the mode.
- *Time Attack:* fixed at **3** KOs — not player-configurable, per the request. 3 echoes the existing best-of-3 motif and, at this game's real time-to-kill, lands a match around 1-3 minutes, matching the pacing everything else is tuned to.
- *Collapse reused as the safety valve for both.* If a continuous match somehow stalls until the walls slam shut, it resolves on that mode's own score (held seconds / KO count, tie = draw) rather than grinding both fighters down — which, with respawns, would never actually resolve. The game-over title reports each mode's own score rather than round pips it never used, and coin payouts route through per-mode keys (same numbers today, but separable).
- **A layout bug caught by looking at the screenshot, not by the assertions.** The first pass drew the mode readouts at the round-pip row (y≈56). The per-player HP panels occupy y 46-108 out to 420px wide, and in split-screen this shared block is re-drawn *centred in each half* — so a wide centred readout lands straight on top of a panel, which it did. Moved both mode readouts below the panels (y≥120); the Zone bars now grow from the outside inward so "who's closer to winning" reads as two bars racing toward the contested centre.
- **Two more TDZ errors, same family as every prior one,** both caught immediately because `window.ACDebug` failed to appear: `shopSide` (Batch 21) and then `MATCH_MODES` were declared with `let`/`const` down beside the code that uses them most, but menu code runs during load and reads both. Fixed by moving both declarations up with the other early state, each with a comment naming the trap. That's now four instances of this exact bug in this file's history — the pattern is unambiguous: **anything the load-time menu build touches must be declared before it, regardless of where it thematically belongs.**
- Verified with `modescheck.js` (**36 assertions**), which drives real matches through the actual game loop rather than poking state: the picker lists three modes with exactly one active and relabels its button; in Zone Control the ring builds, occupancy is detected, a sole occupant banks time while the other doesn't, a contested ring banks nothing for anyone, reaching the target ends the match with the leader as winner, and the ring is disposed at match end; in Time Attack a KO credits the killer and **continues** the match with the victim respawned at full HP at their spawn with i-frames and no stale hitstun/burn/projectiles, and the deciding KO ends it; and — the regression guard that matters most — **Classic is unaffected**: not continuous, no ring, and a KO still advances the round rather than respawning. All prior suites green, zero console errors. Tutorial gained a Game Modes card per the standing tutorial-sync rule.

**Batch 16 — Co-op third-combatant infrastructure.** The structurally riskiest change in this whole arc, deliberately landed last on a codebase that was green everywhere else. `player1`/`player2` were hardcoded as exactly-two throughout, so a co-op boss needed real seams rather than an extra variable.
- **The hard part wasn't the win check — it was target resolution.** Combat is single-opponent-shaped much deeper than it looks: `update()` passes one `opponent` into `tryStartAction()`, which **stores** it on the fighter (`atkOpponent`) at the moment startup begins, and `doBasicAttack`/`doSpecial` resolve against that stored reference much later; `tickEffects()` likewise tests in-flight projectiles against exactly that one opponent. A fighter was therefore structurally incapable of hitting anyone but "the other player." Two helpers are the seam: `pickTarget(attacker, defaultOpponent)` (a player aims at the boss; the boss aims at the nearer *live* player) and `getExtraCombatTargets(fighter)` (extra bodies a projectile should also be tested against). **Both are pure pass-throughs outside co-op** — `pickTarget` returns its argument and `getExtraCombatTargets` returns a shared empty array — so classic/zone/timeattack are byte-for-byte unchanged, which is asserted rather than assumed. Resolved **once** at the top of `update()` into a local rather than at each of the ~9 action call sites, so there's a single place this can be wrong.
  - *One subtlety worth recording:* the first version of the projectile pass added an is-alive guard to the primary target too. That would have quietly changed classic behavior — a shot can currently still land on a fighter who already hit 0 HP during the death sequence, which `endMatch`'s "use the winner decided at KO time, don't re-derive from current hp" comment explicitly depends on. The primary opponent is now tested with *exactly* the original predicate; only the co-op extras get the liveness guard.
- *Rendering:* a new `LAYER_BOSS_BODY` that **both** player cameras enable unconditionally. It costs nothing outside co-op (nothing is on that layer), and it means the boss is visible to both players without either being able to see through it. The two P1-vs-P2 readability tweaks (P2's darkened materials, P2's ground ring) are explicitly skipped for a boss, which is neither side.
- *The boss reuses `Fighter` wholesale* — same `update()` pipeline, so it inherits movement, gravity, hitstun, the action state machine and every animation for free — with `isBossEntity` set as a plain post-construction property and its config read from a separate **`BOSS_MAP`**. Keeping it out of `CHARACTERS` is load-bearing, not tidiness: `buildGrid` iterates `CHARACTERS` unconditionally, so anything in there becomes a pickable fighter on the select screen. Player upgrade multipliers are skipped for a boss (already guarded in Batch 12), so boss/wave scaling is the only multiplier in play and the two systems can't double-dip. Deliberately **not** fed through `resolvePlayerCollision`: a much larger body shoving players around adds feel risk for no requested benefit, so it overlaps rather than pushes.
- *PvE gets its own result path* (`endCoopMatch`), because `endRound`/`endMatch`'s `roundWins`/pips/"player1 beat player2" are all *versus* concepts — reusing them would have printed "Kaelen (Player 1) Wins" about your own teammate. One attempt, one outcome: "Victory!"/"Defeat", a PvE summary, and a **team** coin payout (same to both players, since both fought the same fight). The menu framing follows: the VS badge becomes **TEAM** and the mirror-match note is suppressed, since neither means anything between allies.
- *Deliberate scope cut, flagged not half-built:* if **either** player goes down, the run ends. Real down-but-revivable co-op needs a player-facing sub-state (spectating, revive timers, a revive interaction) that doesn't exist anywhere in this file — a bigger feature than the boss itself.
- **A design bug caught by looking at the screenshot, not by any assertion:** Arena Collapse was still running in co-op, and the crush would have killed the team at ~56s — long before two players could work through a 3000+ HP boss, making the mode unwinnable by construction (and guaranteeing a loss on an open-ended Survival run). Collapse is now disabled in co-op: the boss *is* the pressure there. Its HUD block is skipped too, since the boss bar is the only progress readout PvE needs.
- *Two test-methodology fixes worth recording,* both cases of the harness being wrong rather than the code: (1) pinning fixed coordinates before a frame isn't enough to hold a fight in place — the simulation moves everyone *after* the assignment, and the boss's AI circled behind P1, putting itself outside the swing cone; the check now re-places and re-aims P1 at the boss's **live** position each frame, so it tests hit resolution rather than whether P1 can chase. (2) A first attempt polled for `FIGHT` while already *in* `FIGHT`, exiting before the KO it was waiting for — the mode checks now wait for `DEATH` first.
- Verified with `coopcheck.js` (**30 assertions**) driving real matches: the boss spawns on its own layer, both cameras see it, it has a boss-sized HP pool, and it is **not** in the pickable roster; a player's attack lands on the boss and **not** on their teammate (friendly fire off); the boss damages a player and re-targets to whoever is nearer; killing it ends the run as Victory with no versus framing, and either player falling ends it as Defeat; a cleared Survival wave advances the counter and spawns a tougher challenger **without** ending the run; and — the guard that matters most — **PvP is untouched**: not co-op, no boss entity, and a KO still runs the versus death/round path. All eight other suites still green, zero console errors.

**Batch 17 — Karrigos, the Hollow Titan.** The purpose-built co-op boss, replacing Batch 16's scaled-roster stand-in.
- *Built on the existing rig, not new geometry.* `buildKarrigosMesh` uses the same `buildHumanoidBase` every roster character uses, at 1.5x scale with heavy proportions (broad torso, thick limbs, small head) — so it inherits the shoulder pivots, hand sockets and every animation hook for free, and only the silhouette and materials are new work. A golem is also thematically off-roster (none of the ten are giants), so it reads as a boss without a new art pipeline. Stone fists are built inline rather than via `buildFistProp`, which bolts on bright gold emissive knuckles that are right for Ignis' burning gauntlets and wrong for rock.
- *Config lives in `BOSS_MAP`, not `CHARACTERS`* — load-bearing, since `buildGrid` iterates `CHARACTERS` unconditionally and anything there becomes a pickable fighter. Slow (speed 1.5, so you can always disengage), long reach, high per-hit damage, and a `FRAME_DATA` entry with startups of **26/38** frames against the slowest roster fighter's 16 — the long tell *is* the design.
- *Three attacks on a fixed cycle* (`special: 'titan'` → Ground Slam → Charge → Ember Nova), so the fight has a rhythm you can learn rather than random pressure. Each is built from mechanics already in the file: the slam reuses the shockwave-ring + radial-launch pattern, the charge reuses the `teleportTo` gap-close every dash special uses, and the nova reuses the ordinary projectile struct (so its shots collide with geometry and can be dodged like any other). All of them resolve through Batch 16's target seam, so they hit **both** players where that makes sense — an attack only one player had to respect wouldn't create a shared problem to solve. Its basic melee sweep was likewise widened to catch anyone standing in the cone, so two players couldn't stand shoulder to shoulder with one immune.
- *Difficulty reuses `BOT_DIFFICULTY`'s existing tiers* — no parallel system and no new UI; the difficulty cycler already on the select screen drives it. HP ×5→×11, damage ×1→×1.75, and a new `telegraphMult` that scales **only the wind-up** (never the active or recovery windows, so a harder boss is harder to *read* without shrinking the punish window that makes it fair).
- **A balance bug the checker caught:** the first telegraph scaling (0.15/tier) dropped Insane's basic tell to **14.3 frames — shorter than Draven's 16**, making the boss's attacks *less* readable than an ordinary fighter's, the exact opposite of the design. Retuned to 0.10/tier, flooring it at 18.2, still the longest tell in the game. This is now an explicit assertion rather than a hope.
- **Three visual iterations, each driven by an actual screenshot, worth recording because the cause was non-obvious every time.** (1) The fists rendered as pale cream plastic — `buildFistProp`'s hardcoded gold knuckles. (2) The whole body rendered as beige plastic despite a mid-grey albedo: the arenas are brightly lit (sun + hemisphere + ambient) and the renderer tone-maps at exposure 1.1, which multiplies a lit diffuse surface roughly 3x here, so "mid-grey" became pale tan. Fixed by pitching the palette down to match the existing dark trim (`#3f2a20`) that already reads as dark under that lighting. I confirmed the materials were actually being applied by probing them at runtime rather than guessing from pixels — which ruled out a config bug and pointed at lighting. (3) The ember seams and eyes still washed out to pale yellow: a *lit* emissive material can't win against that much light, so they're now an **unlit `MeshBasicMaterial`**, which is the only way to guarantee the colour on screen is the colour chosen. Also found and fixed a related trap: `addRimOutline` uses an unlit basic material at 25%-lightened body colour, which on a 1.5x boss with chunky limbs became a thick pale border — it now takes optional colour/scale overrides and the boss gets a dark, tighter rim. **The look is a reasoned pass verified by screenshot, not an art-directed result** — proportions, palette and ember placement would all benefit from a real eye.
- *Three test-sequencing bugs fixed in the checkers* (harness wrong, not code): the Ember Nova check sampled projectile count *after* the shots had already connected and despawned (now tracks the peak); the co-op victory check ran against a match Karrigos had **already ended as a Defeat** by killing a player during the preceding damage check (now starts a fresh match); and the PvP-unaffected check insisted on `ROUND_END` when `DEATH` already proves the versus path ran (co-op never enters it at all) and was just timing out mid-animation.
- Verified with `bosscheck.js` (**22 assertions**): the boss builds with its mesh/embers/enlarged hurtbox and is absent from the roster; all three cycled moves enter a startup phase, have a long readable wind-up, rotate through all three, at least one hits both players, and one fires projectiles; its melee catches two adjacent players; HP/damage rise and the telegraph shortens across all four tiers while staying longer than any roster fighter's; and a **full fight terminates in a win at both the easiest and hardest tiers** rather than stalemating. All nine other suites still green, zero console errors. Tutorial gained a co-op card.

**Batch 18** (Survival Waves polish + verification) — in progress.

---

## Verification checklist (run at the end of every batch)

Unchanged from the original brief — these all require an actual browser session (input, visuals, timing, a human on the other side of the bot) that this pass couldn't run. Left unchecked deliberately rather than self-assessed as done; see "Final status" above.

- [ ] No console errors across a full match, including death sequence and a rematch
- [ ] Identical behavior at 30fps, 60fps, and uncapped
- [ ] Ten consecutive rematches, no GPU memory growth
- [ ] Every character's attack/special/skill fires, connects, shows an effect
- [ ] Nobody spawns inside geometry on any of the four maps
- [ ] Teleports and knockback never place a fighter inside a pillar or platform
- [ ] Correct winner declared, including when the winner is hit during the death sequence
- [ ] Bot plays a competent match against every character on every map
- [ ] Menu, pause, and gameover screens reachable/escapable by keyboard alone
- [ ] Window resize mid-fight doesn't shift hitboxes or break the HUD

---

## Out of scope (not to be worked unless explicitly requested)

Online multiplayer, replacing Three.js, splitting into modules, asset pipelines/model loading, new art direction, more characters, more maps.
